# บันทึกการตัดสินใจ (Decision Log)

เอกสารนี้เก็บทุกการตัดสินใจที่ตกลงกันแล้วระหว่างการออกแบบ พร้อมที่มาว่ามาจากคำตอบของเจ้าของ หรือใช้ค่าที่แนะนำ
ข้อมูลประกอบการตัดสินใจอยู่ใน [../research/](../research/)

สถานะ: 🟢 ตกลงแล้ว · 🟡 รอยืนยันในรอบถัดไป

---

## รอบที่ 1 — 17 ก.ย. 2026

| # | หัวข้อ | การตัดสินใจ | ที่มา |
|---|---|---|---|
| D1 | ขนาดร้าน | 🟢 ร้านเปิดใหม่ 1 สาขา ดำเนินการโดยเจ้าของ 2 คน (เจ้าของ + แฟน) · ไม่ทำ multi-tenant แต่ schema เผื่อ `branch_id` ไว้ | คำตอบ Q1 (ใช้ค่าแนะนำ) + Q7 |
| D2 | ฮาร์ดแวร์ | 🟢 ยังไม่ต่ออุปกรณ์ในระยะแรก แต่มีแผนจะต่อ → ห้ามออกแบบที่ผูกกับอุปกรณ์ตัวใดตัวหนึ่ง | คำตอบ Q2 |
| D3 | ผู้พัฒนา | 🟢 เจ้าของเป็น developer เอง ถนัด React / Next.js / NestJS พร้อมเรียนภาษาใหม่ ต้องการ stack ที่เร็ว ทันสมัย เครื่องมือครบ | คำตอบ Q3 |
| D4 | การพิมพ์ | 🟢 ใช้สถาปัตยกรรม print agent (เซิร์ฟเวอร์สร้างคำสั่ง + ตัวกลางที่ร้านดึงงานไปพิมพ์) **แต่เลื่อนไปทำภายหลัง** | คำตอบ Q4 |
| D5 | ลำดับความสำคัญ | 🟢 **เน้น 2 เรื่องก่อน: ระบบสต็อก และ รายการซื้อขาย** | คำตอบ Q4 |
| D6 | การทำงานออฟไลน์ | 🟢 offline-first: เขียนลงฐานข้อมูลในเครื่องก่อนเสมอ แล้ว sync ภายหลัง · ข้อมูลธุรกรรม insert อย่างเดียว + UUID ที่เครื่องสร้าง | คำตอบ Q5 (ใช้ค่าแนะนำ) |
| D7 | การรับเงิน | 🟢 เงินสด + QR PromptPay แบบใส่ยอด (สร้างเอง) · การยืนยันด้วยบริการตรวจสลิป (SlipOK ระดับฟรี) · บัตรไม่ใช้ EDC | คำตอบ Q6 (ใช้ค่าแนะนำ) |
| D8 | ภาษี | 🟢 ยังไม่จด VAT · จะจดเมื่อยอดถึงเกณฑ์ → **ออกแบบ "พร้อม VAT แต่ปิดไว้"** · อัตราภาษีเป็น config ที่มีวันที่มีผล · เลขบิลเรียงต่อเนื่องแก้ไม่ได้ · ห้ามลบบิล · มีรายงานรายรับสะสม 12 เดือนย้อนหลัง | คำตอบ Q7 |
| D9 | ความลึกของสต็อก | 🟢 ตัดวัตถุดิบตามสูตร + คิดต้นทุนต่อแก้ว + เตือนของใกล้หมด · แยกเป็นโมดูล · เริ่มจากวัตถุดิบมูลค่าสูงก่อน · การนับจริงคือความจริงสำหรับต้นทุน ตัวเลขที่ตัดอัตโนมัติคือการพยากรณ์ | คำตอบ Q8 (ใช้ค่าแนะนำ) |
| D10 | เดลิเวอรี | 🟢 ยังไม่ขายตอนนี้ แต่อนาคตจะมี → **ช่องทางขาย (channel) อยู่ใน schema ตั้งแต่วันแรก** แต่ยังไม่สร้างหน้าจอ/รายงานเดลิเวอรี | คำตอบ Q9 |
| D11 | เดดไลน์ | 🟢 **ไม่มีเดดไลน์** ทำเองใช้เอง | คำตอบ Q10 |
| D12 | เกณฑ์คุณภาพ | 🟢 **ความถูกต้อง + ความเร็วของระบบ + การตรวจสอบย้อนหลังแบบละเอียด** อยู่เหนือจำนวนฟีเจอร์ | คำตอบ Q10 |

### ผลที่ตามมาจากรอบที่ 1

- **ไม่ใช้ POS สำเร็จรูประหว่างทาง** — ไม่มีเดดไลน์ จึงสร้างเองได้เต็มที่
- **อุปกรณ์ต่อพ่วงทั้งหมดเลื่อนออกไป** → ระยะแรกไม่ต้องตัดสินใจเรื่องเครื่องพิมพ์ ลิ้นชัก หรือ OS ของแท็บเล็ต
- **"ความถูกต้อง" เป็นข้อกำหนดหลัก** → ตรรกะเรื่องเงินและสต็อกต้องอยู่ในที่เดียว มีเทสต์แบบ property-based และมีการตรวจ invariant อัตโนมัติ
- **ร้าน 2 คน** → ระบบสิทธิ์ไม่ต้องซับซ้อน แต่**ทุกการกระทำยังต้องระบุตัวคนทำ** เพื่อให้ตรวจสอบย้อนหลังได้

---

## รอบที่ 2 — 17 ก.ย. 2026

| # | หัวข้อ | การตัดสินใจ | ที่มา |
|---|---|---|---|
| D13 | Tech stack | 🟢 **TypeScript ทั้งระบบ** · monorepo pnpm + Turborepo · `packages/domain` ตรรกะเงิน/สต็อกชุดเดียว · หน้าจอ React + Vite (PWA) + SQLite WASM/OPFS · **backend NestJS** (ไม่ใช่ Hono) + PostgreSQL + Drizzle · Zod · Vitest + fast-check + Playwright | คำตอบ Q11 — เลือก NestJS ที่ถนัด |
| D14 | เซิร์ฟเวอร์ | 🟢 มีเซิร์ฟเวอร์ตั้งแต่เฟสแรก แต่ทำแค่ รับ sync · รายงาน · สำรองข้อมูล · **ต้องใช้บริการฟรี** · การขายทำงานในเครื่อง 100% | คำตอบ Q12 (ข) |
| D15 | โฮสติ้ง | 🟢 **ไม่ผูกบัตรทุกบริการ** · PWA บน Cloudflare Pages · API บน **Render free (Singapore)** เปิดตลอดด้วย ping ทุก 10 นาที (744/750 ชม.) · DB บน **Neon** free (Singapore, 0.5 GB) · backup รายคืนด้วย GitHub Actions → R2 · **กฎ: sync เฉพาะเวลาเปิดร้าน** และ `/health` + poll ไม่แตะ DB เพื่อไม่เกิน 100 CU-ชม. ของ Neon · (Cloud Run ตัดออกเพราะต้องผูกบัตร) | คำตอบ Q30 (แก้ไข 17 ก.ย.) + [../research/10-โฮสติ้งฟรี.md](../research/10-โฮสติ้งฟรี.md) |
| D16 | โครงสร้างเมนู/สูตร | 🟢 ใช้ข้อมูลจริงจาก `DA-YO_เมนู.xlsx`: 24 เมนู × 3 ขนาด (16/20/22 oz) × 5 ระดับหวาน (0–100%) · **สูตรเป็นตารางชัดแจ้งต่อ (เมนู, ขนาด, ความหวาน)** นำเข้าจากไฟล์ · ความหวานเป็นมิติของสูตร ไม่ใช่ modifier · ยังไม่มีร้อน / ระดับน้ำแข็ง / ท็อปปิ้ง / เปลี่ยนนม (schema เผื่อ modifier ไว้) · ราคาเป็นตัวเลขต่อขนาด | คำตอบ Q13 + [../research/09-เมนูและสูตรจากไฟล์ร้าน.md](../research/09-เมนูและสูตรจากไฟล์ร้าน.md) |
| D17 | เบสและซับสูตร | 🟢 เบส 7 ตัว (ชาไทยเบส · ชาเขียวเบส · เบสมะพร้าว · น้ำเชื่อม · น้ำผึ้งเจือจาง · ครีมชีสโฟม · มัทฉะช็อต) เป็น **item แบบ prepared** มี BOM + yield + อายุ · มีหน้าจอ "ทำเบส" ที่ตัดของดิบและเพิ่มเบสเข้าสต็อก · ต้นทุนเบสคิดจาก batch จริง | คำตอบ Q14 (ต้มเป็น batch ตามไฟล์) |
| D18 | จังหวะตัดสต็อก | 🟢 **ตัดตอนชำระเงิน** · ยกเลิกบิลต้องตอบว่า "ทำไปแล้วหรือยัง" → ยังไม่ทำ = คืนวัตถุดิบ · ทำแล้ว = บันทึกของเสีย | ใช้ค่าแนะนำ Q15 |
| D19 | การรับของเข้า | 🟢 บันทึกการซื้อทุกครั้ง (ร้านค้า · จำนวนหน่วยซื้อ · ราคารวม · รูปใบเสร็จ) · ต้นทุนต่อหน่วยใช้ = ถัวเฉลี่ยเคลื่อนที่ · ก่อนมีบันทึกซื้อใช้ราคาตั้งต้นจากไฟล์ | ใช้ค่าแนะนำ Q16 |
| D20 | นับสต็อก | 🟢 นับจริง **สัปดาห์ละครั้ง** เฉพาะของดิบมูลค่าสูง (~10 รายการ) นับเป็นหน่วยซื้อแล้วระบบแปลง · รายงานส่วนต่างทฤษฎี-จริงเป็นบาท · น้ำแข็ง/น้ำ/หลอด: ไม่นับสต็อก แต่ยังคิดต้นทุน (`is_tracked=false`) | ใช้ค่าแนะนำ Q17 |
| D21 | ระดับการตรวจสอบ | 🟢 บัญชี + PIN แยกคน (เริ่ม 2 คน: TungAo, DCm) · `order_event` แบบต่อท้ายอย่างเดียว ล่ามด้วยแฮช · แก้ราคา/สูตร/เมนู เก็บค่าเดิม-ใหม่ · ห้ามลบบิล ยกเลิก/ส่วนลดต้องใส่เหตุผล · งานตรวจ invariant อัตโนมัติทุกคืน | ใช้ค่าแนะนำ Q18 |
| D22 | กะและเงินสด | 🟢 **มี shift ใน schema ตั้งแต่แรก** (เผื่อจ้างพนักงาน) แต่ตอนนี้ใช้แบบ "1 กะ = 1 วันทำการ" เปิดร้านใส่เงินทอน ปิดร้านนับแยกชนิดธนบัตร · Z report เป็น snapshot แก้ไม่ได้ | คำตอบ Q19 |
| D23 | ขอบเขตเฟส 1 | 🟢 เมนู+ตัวเลือก · ขายผ่านตะกร้า · เงินสด + QR PromptPay ยืนยันเอง · สูตร+ตัดสต็อก · ทำเบส · รับของเข้า · นับสต็อก · ปิดวัน/Z · audit · รายงาน (รายวัน · ขายดี · margin ต่อแก้ว · ส่วนต่างสต็อก · รายรับสะสม 12 เดือน) · **เลื่อน:** เฟส 2 SlipOK · เฟส 3 พิมพ์ · เฟส 4 เดลิเวอรี · ไม่ทำ: สมาชิก/โปรโมชั่น | คำตอบ Q20 |
| D24 | การนำเข้าจาก Excel | 🟢 นำเข้า **ครั้งเดียว** ด้วยสคริปต์ (สินค้า 32 · BOM 47 · สูตร 360 · ราคา 72 · อุปกรณ์ 15) → หลังจากนั้น POS เป็นแหล่งความจริง ไม่ sync กลับ Excel · R&D Log / SOP / คู่มือ คงไว้ใน Excel | ค่าแนะนำจากการอ่านไฟล์ |

### ผลที่ตามมาจากรอบที่ 2

- **แบบร่าง modifier ที่มี `factor` ตัวเลข (จาก 05-โครงสร้างข้อมูลฯ) ถูกแทนที่** ด้วย recipe ต่อ (variant, sweetness) เพราะสูตรจริงไม่ได้ scale เชิงเส้นตัวเดียว
- **มีชั้น "prepared item"** เพิ่มจากที่เคยออกแบบ → หน้าจอทำเบส + สต็อกเบส + วันหมดอายุ
- **เงื่อนไขโฮสติ้งฟรีกำหนดพฤติกรรม sync** (ไม่ sync ตอนปิดร้าน) → ต้องอยู่ใน spec
- NestJS แทน Hono → ใช้ `@nestjs/*` + Drizzle (ไม่ใช่ TypeORM/Prisma) เพื่อแชร์ schema กับฝั่งเครื่อง

---

## รอบที่ 3 — 17 ก.ย. 2026

| # | หัวข้อ | การตัดสินใจ | ที่มา |
|---|---|---|---|
| D25 | สั่งผ่าน LINE | 🟢 **อยู่ในขอบเขต** ลูกค้าสั่งผ่าน **LIFF** (หน้าเว็บของเราในแอป LINE) ต่อ API ของ POS · channel = LINE OA · ออเดอร์สร้างที่เซิร์ฟเวอร์ แต่ **เลขที่บิล/ตัดสต็อกยังเกิดที่แท็บเล็ตตอนกด "รับเงินแล้ว"** · จ่าย QR ก่อนทำ · ยกเลิกอัตโนมัติถ้าไม่จ่ายใน 15 นาที · ใช้ reply message เท่านั้น (ฟรี) push "พร้อมรับ" เป็นตัวเลือกภายในโควตา 300/เดือน · ทำหลัง API เสร็จ (ขั้น 6) | คำตอบ Q29 + [../research/11-สั่งผ่าน-LINE.md](../research/11-สั่งผ่าน-LINE.md) 🟡 รายละเอียดรอยืนยัน Q31–Q34 |
| D26 | วิธีเก็บสูตร | 🟢 ตารางชัดแจ้ง 360 แถวนำเข้าจากไฟล์ + ปุ่มช่วยสร้าง 20/22 oz จาก 16 oz ด้วยตัวคูณ | ใช้ค่าแนะนำ Q22 |
| D27 | หมวดหน้าจอขาย | 🟢 ชาไทย / ชาเขียว / มัทฉะพรีเมียม + แท็บ "ขายดี" อัตโนมัติ · ค่าเริ่มต้น 16 oz + 50% | ใช้ค่าแนะนำ Q23, Q24 |
| D28 | เบสติดลบ | 🟢 ไม่บล็อกการขาย · เตือนแดง · ปิดกะต้องดูรายการเบสติดลบ | ใช้ค่าแนะนำ Q25 |
| D29 | ของที่ไม่นับสต็อก | 🟢 ไม่นับ (แต่คิดต้นทุน): น้ำแข็ง น้ำสะอาด น้ำดื่มถัง เกลือ · นับ: หลอด สติกเกอร์ และที่เหลือทั้งหมด | ใช้ค่าแนะนำ Q26 |
| D30 | ยอดยกมา | 🟢 นับจริงครั้งแรกทั้ง 32 รายการ → movement `OPENING` ที่ราคาตั้งต้น | ใช้ค่าแนะนำ Q27 |
| D31 | คงไว้ใน Excel | 🟢 R&D Log · สรุปการชิม · คู่มือมัทฉะ · SOP · เคล็ดลับ ไม่ย้าย · แต่ product เก็บ `prep_group` และ "เบสที่ต้องมีพร้อม" | ใช้ค่าแนะนำ Q28 |

### ผลที่ตามมาจากรอบที่ 3

- **เซิร์ฟเวอร์กลายเป็นต้นทางของออเดอร์ได้** (LINE) → กฎ "ธุรกรรมไหลขึ้นอย่างเดียว" เปลี่ยนเป็น "ธุรกรรมมี `origin` (device/server) และ **ทุกการเปลี่ยนสถานะยังเกิดที่เครื่องขาย**" · แท็บเล็ตต้อง pull ออเดอร์ขาเข้าขณะเปิดกะ
- **Render ต้องตื่นตลอด** (webhook + LIFF) → ping ทุก 10 นาที · Neon ยังต้องหลับได้ → endpoint ที่ถูก ping/poll ต้องไม่แตะ DB โดยไม่จำเป็น
- spec ต้องเพิ่ม: ตาราง `customer`, ฟิลด์ต้นทาง/สถานะออเดอร์ LINE, โมดูล LINE ใน API, โหมดลูกค้าใน PWA

---

## รอบที่ 4 — 17 ก.ย. 2026

| # | หัวข้อ | การตัดสินใจ | ที่มา |
|---|---|---|---|
| D32 | รายละเอียด LINE | 🟢 สั่งผ่าน **LIFF** (บอทตอบแค่ลิงก์) · **จ่ายก่อนทำ** ร้านกด "รับเงินแล้ว" จึงเข้าคิว/ตัดสต็อก · ไม่จ่ายใน 15 นาทียกเลิกอัตโนมัติ · รับที่ร้านเท่านั้น · push "พร้อมรับ" **เปิดใช้** ระบบนับโควตา 300/เดือนและปิดเองเมื่อใกล้เต็ม · สร้างเป็นขั้นที่ 6 (หลัง API) | คำตอบ Q31–Q34 (ใช้ค่าแนะนำ) |
| D33 | หน่วยต้นทุน | 🟢 ต้นทุนต่อหน่วยใช้เก็บเป็น **micro-satang ต่อหน่วยใช้ (`_usat`, 1 บาท = 100,000,000 usat)** แทน `_milli` ที่ร่างไว้ เพราะราคาอย่าง 0.019925 ฿/ml ต้องเก็บได้ครบ 6 ตำแหน่ง ไม่งั้น golden test กับ Excel ไม่ผ่าน · คูณด้วย BigInt | ตัดสินใจตอนเขียนแผน 1 |

**spec เปลี่ยนสถานะเป็น 🟢 อนุมัติแล้ว** → เริ่ม writing-plans (แผนอยู่ที่ [../superpowers/plans/](../superpowers/plans/))

---

## รอบที่ 5 — 17 ก.ย. 2026 (หลังทำแผน 1)

| # | หัวข้อ | การตัดสินใจ | ที่มา |
|---|---|---|---|
| D34 | ต้นทุนมาตรฐานของวัตถุดิบ | 🟢 ใช้ **ต้นทุนเฉลี่ยจากบันทึกซื้อ** (ชีต "สินค้าและสต็อก" คอลัมน์ "ต้นทุนเฉลี่ยต่อหน่วยซื้อ") ไม่ใช่ราคาตั้งต้น — ตรงกับที่ Excel ใช้คิดทุกสูตร · ก่อนมีของเข้าครั้งแรก avg = ค่านี้ | ยืนยันข้อ 3 |
| D35 | ถุงมือ / ปากกาแต้มสี (OT-SUP-01/02) | 🟢 เป็น item ที่ **นับสต็อก** แต่ไม่อยู่ในสูตร · ยอดยกมา (D30) นับจริงครั้งแรกทั้ง **35** รายการ | ยืนยันข้อ 4 |
| D36 | เงินคืนตอนยกเลิกบิลเงินสด | 🟢 บันทึก `cash_movement` kind ใหม่ **`VOID_REFUND`** (แยกจาก `PAID_OUT` ที่คนบันทึกเอง) · expected = opening + cash_sales − void_refunds + paid_in − paid_out − drops | ยืนยันข้อ 5 |
| D37 | อายุใช้งานอุปกรณ์ | 🟢 เก็บ **`life_months` (จำนวนเต็ม)** แปลงจากปีในไฟล์ × 12 ไม่ปัดทิ้ง | ข้อ 6 |
| D38 | แฮช event ของบิล | 🟢 แฮชครอบ `order_id`, `seq` ต่อบิล, `device_id` ด้วย (นอกจาก chain_id/chain_seq) ตาม spec §4.9 | ข้อ 7 |
| D39 | `VOID_WASTE` | 🟢 ลบออกจาก spec — ของที่ทำแล้วโดนยกเลิกไม่มี movement เพิ่ม ใช้ marker `waste` ใน event `VOIDED` · movement kind มี 11 ค่า | ข้อ 8 |
| D40 | spec §9 | 🟢 อัปเดตตามไฟล์ล่าสุดและโค้ด: ของดิบ 35 · อุปกรณ์ 32 · ไม่นับสต็อกตาม D29 | ข้อ 9 |
| D41 | เมื่อไฟล์ Excel ของร้านเปลี่ยน | 🟢 fixture ใน repo คือ snapshot · อัปเดต fixture/นำเข้าใหม่ **เฉพาะเมื่อเจ้าของสั่ง** และต้องตรวจตัวเลขคาดหวังทั้งหมดใหม่ทุกครั้ง | ข้อ 10 |
| D42 | วิธีทำงาน | 🟢 มีสิทธิ์จัดการ branch และ push ได้ **หลัง audit + code review เสมอ** · ทำทุกแผนต่อเนื่องจนจบ แต่ละแผนต้องยืนยันว่าใช้งานได้จริงและไม่มี issue ค้างก่อนเริ่มแผนถัดไป · มีข้อสงสัยต้องถามเจ้าของเสมอ · แยก branch ทำคู่ขนานได้ แล้ว review/audit ก่อนรวม | คำสั่งเจ้าของ + ข้อ 11 |
| D43 | เลือกโมเดล agent | 🟢 sonnet เป็นขั้นต่ำสำหรับ implement/review ราย task · opus สำหรับ final review ของแต่ละแผนและการเขียนแผนใหม่/งานที่ต้องคิดซับซ้อน · ไม่ใช้ haiku | ข้อ 12 |

### ผลที่ตามมาจากรอบที่ 5

- แผน 2 ต้องแก้ก่อนเริ่ม: ตัวเลขจากไฟล์ใหม่ · `cash_movement.kind` เพิ่ม `VOID_REFUND` · `equipment.life_months` · `order_event` แฮชครอบ order_id/seq/device_id
- โค้ดแผน 1 ที่ต้องปรับตาม (ทำในแผน 2 เพราะเป็น interface ที่ DB ใช้): `CashInputs` เปลี่ยนเป็น `voidRefundsSatang` · contracts `CashMovementKind` เพิ่ม `VOID_REFUND` · seed `lifeYears` → `lifeMonths` · domain `EventCore` เพิ่ม orderId/seq/deviceId
- push ขึ้น GitHub ต้องรอเจ้าของเพิ่ม permission (`Bash(git push origin *)`) และติดตั้ง/ล็อกอิน `gh`

---

## รอบที่ 6 — 18 ก.ย. 2026

| # | หัวข้อ | การตัดสินใจ | ที่มา |
|---|---|---|---|
| D44 | แบรนด์และโลโก้ | 🟢 ใช้ชุดโลโก้เวกเตอร์ `OneDrive/Documents/DAYO/DA-YO_logo_vector/` (primary / reversed / 1color-deep / white / appicon 16–1024) · พาเลต: **Deep Forest `#1E3B22`** (หลัก/ตัวอักษร) · **Matcha `#7EA05F`** (เฉพาะน้ำชาในถ้วย ห้ามเป็นตัวอักษรบนครีม) · **Cream `#F6F3E8`** (พื้น) · สีป้ายร้าน: ส้ม `#ED7A13` · ครีมป้าย `#FFFADD` · แอปออกแบบตามเทมเพลตใบสั่งงานป้าย (พื้นเขียวเข้ม · แถบคลื่นส้ม/เขียว · ตัวอักษรครีม) · ไอคอน PWA ใช้ `appicon_cream` · โลโก้เล็กกว่า ~25 มม. ตัดสโลแกน | คำตอบ Q3-19 (แทนค่าแนะนำเดิมสีส้ม `#c2410c`) |
| D45 | กติกา commit | 🟢 ใช้ skill `committing-code` ทุกครั้ง · **ไม่มี trailer ของ AI** ใน commit/PR (ลบจากประวัติเก่าที่ยังไม่ push แล้ว) · **เฉพาะโปรเจกต์นี้** เอกสาร `docs/**` (research/design/spec/plan/คำถาม/บันทึกการทำ) commit เข้า git ได้ · ไฟล์ทำงานชั่วคราว (`.superpowers/`, ledger, review, `.claude/`) ไม่เข้า git | คำตอบข้อ 10–11 |
| D46 | แท็บเล็ต | 🟢 Android + **Chrome** · รุ่นยังไม่กำหนด → spike แผน 3 ทดสอบบน Chrome Android ที่มี และต้องทดสอบซ้ำเมื่อได้เครื่องจริง | คำตอบ Q3-1 |
| D47 | ใช้ค่าแนะนำ (คำสั่ง "ทำต่อเลย" 18 ก.ย.) | 🟢 **แผน 2 ข้อ 1** ออเดอร์ LINE ก่อนจ่าย: เซิร์ฟเวอร์ออก `seq` · รับเงิน/ปฏิเสธต้องออนไลน์ · หลังจ่ายแท็บเล็ตเขียนอย่างเดียว · **ข้อ 2 = Q3-13** ยกเลิกได้เฉพาะบิลใน **กะที่เปิดอยู่** (บิลเก่าคืนเงินด้วยมือ) · **ข้อ 3** ต้นทุนของไม่นับสต็อกแก้เองในหน้าจัดการ + audit_log + เตือนเมื่อราคารับเข้าต่างเกิน 10% · **ข้อ 4** ต้นทุนมาตรฐานเบส/ชุดบรรจุภัณฑ์คำนวณใหม่อัตโนมัติเมื่อส่วนประกอบหรือ BOM เปลี่ยน + audit_log · **ข้อ 5** `unique(device_id, business_date, queue_no)` · **ข้อ 6** partial unique เปิดกะได้ครั้งละหนึ่งต่อเครื่อง · **ข้อ 7** อนุญาต CHECK constraint (เช่น `VOID_REFUND` ⇔ มี order_id, จำนวนเงิน > 0) · **ข้อ 8** ตาราง R ทุกตารางมี `version` + `updated_at` · **ข้อ 9** `setting` คีย์ `(key, effective_from)` | คำตอบ "ทำต่อเลย" |
| D48 | แผน 3 (ใช้ค่าแนะนำ) | 🟢 Q3-2 spike ผ่าน Cloudflare Quick Tunnel · ขายจริงหลังแผน 3 deploy Pages เป็นงานเล็กแยก · Q3-3 พร้อมเพย์กรอกในหน้าตั้งค่าครั้งแรก ไม่อยู่ใน repo · Q3-4 QR ใส่ยอดทุกบิล · Q3-5 ไม่พิมพ์ แสดงเลขคิว+เลขบิลบนจอ · Q3-6 ส่วนลดเป็นบาท บังคับเหตุผล คนที่ล็อกอินอนุมัติ ลบรายการจนต่ำกว่าส่วนลด→ลบส่วนลด · Q3-7 ตะกร้าในหน่วยความจำ บันทึกตอนชำระ · Q3-8 รวมบรรทัด · Q3-9 ขายดี 7 วัน 8 เมนู · Q3-10 popup ทุกครั้ง · Q3-11 ไทยใหญ่+อังกฤษเล็ก · Q3-12 เงินทอนไม่มีค่าเริ่มต้น · Q3-14 ปุ่มเหตุผลยกเลิก 3 ปุ่ม + พิมพ์เอง · Q3-15 QR void บังคับเลขอ้างอิง · Q3-16 หน้าตั้งค่าครั้งแรก (TungAo, DCm) · Q3-17 แยกแผน 3b (ปิดกะ/Z/สำรองไฟล์) ห้ามขายจริงก่อน 3b เสร็จ · Q3-18 ข้อความไทยตามร่าง | คำตอบ "ทำต่อเลย" |
| D49 | push | 🟢 เจ้าของ push เอง · agent ไม่ push · ยังต้อง audit + review ก่อน merge เข้า main | คำสั่ง 18 ก.ย. |
| D50 | แผน 3 คำถามรอบ 2 (ใช้ค่าแนะนำ) | 🟢 **Q3-20** ไม่มีบิล 0 บาท: ส่วนลดต้อง **น้อยกว่า** ยอดรวมเสมอ (reducer, หน้าต่างส่วนลด, `commitSale` → `DISCOUNT_TOO_BIG`) · CHECK `payment.amount_satang > 0` คงเดิม · แจก/ชดเชยเครื่องดื่มทำในแผน 4 เป็นการเบิกของออกพร้อมเหตุผล ไม่ใช่ยอดขาย 0 บาท · **Q3-21** ล็อก PIN ทั้งตอนล็อกอินและตอนอนุมัติด้วย PIN เจ้าของ (ตัวนับเดียวกัน **ต่อผู้ใช้**): ผิด 5 ครั้งติด → รอ 30 วินาที · ผิดอีกหลังพ้นเวลา → เท่าตัว (60, 120, 240, 480) **สูงสุด 15 นาที** · ถูกหนึ่งครั้ง → ล้าง · ระหว่างล็อกไม่ตรวจ PIN · ตัวนับเก็บใน `sync_state` (ในเครื่อง ไม่ sync อยู่รอด reload) · ทุกครั้งที่ผิดและทุกครั้งที่ล็อกเขียน `audit_log` (`pin_fail`, `pin_lockout`) ไม่เก็บ PIN · จอแสดง "ลองใหม่ใน N วินาที" · **Q3-22** เจ้าของที่ล็อกอินอยู่อนุมัติ void ของตัวเองได้ แต่ต้องกรอก PIN ซ้ำ + เหตุผลทุกครั้ง (เลือกชื่อไว้ให้ล่วงหน้าได้ ไม่ข้าม PIN) · บิล void แสดงในรายงาน void ประจำวัน (ข้อมูลอยู่ใน event `VOIDED` ที่มี `approvedBy`/เหตุผล — หน้ารายงานอยู่แผน 3b) · **Q3-23** คลื่นใต้แถบบนใช้ส้ม `#ED7A13` + Deep Forest `#1E3B22` / เขียวเข้มอ่อนลง `#35613B` **ไม่มี Matcha** (Matcha อยู่เฉพาะน้ำชาในโลโก้) · สีกลางเพิ่ม `#FFFDF6` `#E4ECD9` `#5C6B5E` `#D6D3C4` ใช้ได้ · พื้นแอป Cream `#F6F3E8` · `#FFFADD` ใช้กับป้ายร้านเท่านั้น · **Q3-24** ล็อก (อัตโนมัติ 10 นาที/กดเอง) แล้ว **เก็บตะกร้าไว้** · **Q3-25** "ขายดี" = **7 วันปฏิทินล่าสุด** (นับจาก business date ของกะที่เปิดอยู่) · **Q3-26** ป้าย "ยังไม่ส่ง N รายการ" นับ **บิล** (order ที่ยังมีแถว outbox ค้าง บิลละ 1 รวมแถวตอน void) ไม่นับแถว · ข้อมูลที่ไม่ใช่บิล (กะ, เงินเข้า/ออก, stock movement ที่ไม่ใช่ของบิล) นับรายการละ 1 ต่อ id เพื่อไม่ให้ป้ายขึ้น 0 ทั้งที่ยังมีของค้างส่ง (`countPendingSyncItems`, `BootstrapState.pendingSyncItems`) · **Q3-27** ราคาเปลี่ยนระหว่างตะกร้า/QR กับตอนชำระ → `commitSale` รับ `expectedTotalSatang` (ยอดที่ลูกค้าเห็น) ถ้ายอดที่คิดจาก DB ไม่ตรง → ปฏิเสธ `PRICE_CHANGED` ไม่เขียนอะไร · หน้าชำระโหลดเมนูใหม่ ปรับราคาในตะกร้า แสดงยอดเดิม → ยอดใหม่ แล้วแคชเชียร์ต้องกดยืนยันอีกครั้ง (QR สร้างใหม่ตามยอดใหม่) | คำตอบ 'เอาตามที่แนะนำ' 18 ก.ย. |
| D51 | ทดสอบบนแท็บเล็ตจริง | 🟢 **เลื่อนไปทำตอนท้ายสุด** (หลังทำแผนอื่นแล้ว) · แผน 3 merge เข้า main ในเครื่องได้โดยยังไม่มีผลแท็บเล็ต · ยังเป็นเงื่อนไข **ก่อนขายจริง**: ตาราง spike S1–S10 + ทดสอบขายจริง (แผน 3 Task 15 Step 4) + ตรวจภาพแบรนด์/หน้าขายบนเครื่องจริง (Q3-28, Q3-29 รวมไว้ในรอบนั้น) · เริ่มแผน 3b ได้เลย (Q3-30) | คำสั่ง 18 ก.ย. "tablet พักไว้ก่อน เอาไว้ทำตอนสุดท้าย" |

---

## รอบที่ 7 — 19 ก.ย. 2026 (คำถามก่อนทำแผน 3b)

| # | หัวข้อ | การตัดสินใจ | ที่มา |
|---|---|---|---|
| D52 | แผน 3b คำถามก่อนทำ Q3b-1 – Q3b-10 (ใช้ค่าแนะนำ) | 🟢 **Q3b-1** นับเงินปิดกะ 9 ชนิดตาม spec §5 (1000/500/100/50/20/10/5/2/1 บาท) ไม่มีเหรียญสตางค์ · **Q3b-2** คนที่ล็อกอินเป็นคนนับ แล้ว **เจ้าของกรอก PIN ซ้ำทุกครั้ง** (เลือกตัวเองได้ เหมือน void — D50 Q3-22) · Z บันทึกทั้ง `countedBy` และ `closedBy` · **Q3b-3** นับแบบไม่เห็นยอด **จริง**: ปุ่ม "ปิดกะ" ย้ายไปอยู่บนหน้าขาย ไม่ผ่านหน้า X · หน้าปิดกะไม่แสดงเงินสดที่ควรมีจนกว่าจะกด "นับเสร็จ" แล้วจึงเห็นยอดที่ควรมีและส่วนต่าง (แก้จำนวนที่นับได้ก่อนยืนยัน) · หน้า X ยังเปิดดูได้ตลอด (spec §4.8) แต่เป็นคนละทางกับการปิดกะ และไม่แสดงบรรทัดเงินสดที่ควรมีระหว่างกะเปิด · e2e ตรวจว่ายอดที่ควรมีไม่ปรากฏก่อนนับ (review I-2) · **Q3b-4** บิลยกเลิกไม่นับในยอดสุทธิ: net = gross − ส่วนลด − ยอดบิลยกเลิก · grand total สะสม net (ใช้เทียบเกณฑ์ VAT — D8) · **Q3b-5** Z แสดงบนจอทันทีหลังปิดกะ + รายการ Z ย้อนหลังในเครื่อง · ไม่ส่งออกแยก (CSV เป็นงานแผน 7) · **Q3b-6** สำรองเป็นไฟล์ SQLite ทั้งไฟล์ ดาวน์โหลดลง Downloads ชื่อ `dayo-pos-A-YYYYMMDD-HHmmss.sqlite3` แล้วเจ้าของย้ายไป Google Drive เอง · ไม่มีปุ่มแชร์ · **Q3b-7** ไม่บังคับสำรอง · แถบแดงที่หน้า Z และหน้าเปิดกะจนกว่าจะ **ยืนยัน** ไฟล์สำรองที่ทำหลัง Z ใบล่าสุด · แถบหายเมื่อเจ้าของกด "บันทึกไฟล์แล้ว" หลังดาวน์โหลดเท่านั้น ไม่ใช่ตอน export (review I-4) · ตอนยืนยันบันทึก `sync_state` + `audit_log` · **Q3b-8** ตั้งแต่ 05:00 เวลาไทยของวันถัดไป หน้าขายขึ้นแถบแดงว่ากะยังไม่ปิด · ขายต่อได้ ไม่บล็อก · **Q3b-9** บันทึกเงินเข้า / จ่ายออก / นำเงินออกไปเก็บ (`PAID_IN`/`PAID_OUT`/`DROP`) ในแผน 3b · ทุกคนที่ล็อกอินบันทึกได้ · บังคับเหตุผล · **Q3b-10** เปิดกะด่วนเฉพาะเจ้าของ · เขียน `audit_log` `quick_open` + ธง `openedQuick` ใน snapshot ของ Z · ไม่แก้ schema · ไม่ต้องกรอก PIN ซ้ำ (หมายเหตุ: เปิดกะปกติด้วยเงินทอน 0 ทุกคนทำได้อยู่แล้ว — D48 Q3-12) | คำตอบ 'เห็นด้วย ทำต่อได้เลย' 19 ก.ย. |
| D53 | แผน 3b คำถามจาก review แผน Q3b-11 – Q3b-13 (ใช้ค่าแนะนำ) | 🟢 **Q3b-11** Z ใบก่อนแฮชไม่ตรง (ถูกแก้หรือไฟล์เสีย) **ยังปิดกะได้**: ระบบตอบรหัสเฉพาะ `Z_CHAIN_BROKEN` ไม่ใช่ "ข้อมูลไม่ถูกต้อง" · เจ้าของกรอก PIN ซ้ำเพื่อรับทราบ · Z ใบใหม่มีธงเตือนถาวร (`chainWarning` ใน snapshot ที่มีแฮชคุม) แสดงที่หน้า Z และรายการ Z ย้อนหลัง · grand total ต่อจากค่าที่ **คำนวณใหม่** จาก snapshot ทุกใบเรียงตาม `zNo` (Σ net) ไม่ใช่ค่าที่เก็บไว้ · เขียน `audit_log` `z_chain_broken_ack` · แทน T3b-6 เดิมที่ให้ปฏิเสธการปิดกะ (review I-3) · **Q3b-12** หน้าปิดกะแสดงพร้อมเพย์รับ / โอนคืน / สุทธิ + ช่องกรอกยอดพร้อมเพย์จากแอปธนาคาร · ส่วนต่าง (ธนาคาร − พร้อมเพย์สุทธิ) แช่แข็งใน snapshot ของ Z · ช่องนี้ **ไม่บังคับ และไม่มีเกณฑ์/เหตุผล** (review ไม่ได้เสนอเรื่องนี้ — agent เลือกให้ ถามเปลี่ยนได้) · **Q3b-13** สำรองไฟล์ได้ **เฉพาะเจ้าของ** (บังคับทั้ง API และหน้าจอ) · ยอมรับว่าไฟล์มี `pin_hash` และหมายเลขพร้อมเพย์ของร้าน · ไม่เข้ารหัส | คำตอบ 'เห็นด้วย ทำต่อได้เลย' 19 ก.ย. |
| D54 | แผน 3b คำถามจาก review Task 6 รอบ fix 1b Q3b-14 – Q3b-17 (ใช้ค่าแนะนำ) | 🟢 **Q3b-14** จ่ายเงินออก / นำเงินออกไปเก็บ (`PAID_OUT`/`DROP`) ที่ทำให้เกินเงินสดที่ควรมีในลิ้นชัก ณ ขณะนั้น **ยังบันทึกได้** ไม่บล็อก · หน้าจอเตือนด้วยข้อความยืนยันก่อนบันทึกจริง (ให้ Task 11 ทำ) · **Q3b-15** ยอมรับข้อจำกัดของการนับแบบไม่เห็นยอด (Q3b-3 · D52) สำหรับร้าน 2 คน: คนนับที่จำยอดขายระหว่างวันได้เองอาจคาดเดายอดที่ควรมีได้คร่าวๆ ก่อนกดนับเสร็จ · หน้า X แสดงบรรทัดเงินสดแต่ละองค์ประกอบอยู่แล้ว (เงินทอนตั้งต้น/เงินสดขาย/คืนเงินสด/เงินเข้า/เงินออก/นำออกเก็บ) ซึ่งพอให้คาดเดาได้ · บันทึกไว้เป็นข้อจำกัดที่รับทราบแล้ว ไม่ต้องแก้เพิ่ม · **Q3b-16** Z ใบก่อนแฮชไม่ตรง **หรือ** chain ก่อนหน้าคำนวณใหม่แบบเข้มไม่ได้เลย (net ที่เก็บไว้ไม่ตรงกับ gross−ส่วนลด−ยอดยกเลิก, `zNo` มีช่องว่างหรือซ้ำ, snapshot อ่านไม่ได้) ครั้งแรกตอบ `Z_CHAIN_BROKEN` เสมอ ไม่ใช่ `BAD_INPUT` — เจ้าของกรอก PIN ซ้ำรับทราบแล้วปิดกะต่อได้เสมอ ไม่มีทางถูกบล็อกค้างอีก โดยคำนวณ chain แบบผ่อนปรนที่ไม่มีวันโยน error (`recomputeZChainLenient` ใน `@dayo/domain`): net ของ Z ก่อนหน้าแต่ละใบ = gross−ส่วนลด−ยอดยกเลิก ถ้าตัวเลขสามตัวนี้เป็นจำนวนเต็มไม่ติดลบและสอดคล้องกัน ไม่เช่นนั้นใช้ net ที่เก็บไว้ถ้าอ่านได้เป็นจำนวนเต็ม ไม่เช่นนั้นถือเป็น 0 · zNo ใหม่ = จำนวนแถว Z ที่มีอยู่ทั้งหมด + 1 (เรียงตาม zNo ที่อ่านได้ ถ้าอ่านไม่ได้ใช้ลำดับแถวเดิม) · `chainWarning` (type `ZChainWarning`) เพิ่มช่อง `unreadableZs: { shiftId, zNo }[]` บันทึกรายการ Z ที่อ่านไม่ได้/ไม่สอดคล้องไว้ถาวรด้วย · **Q3b-17** `SHIFT_CHANGED` ต้องเกิดเมื่อ **ตัวเลขใดๆ ในรายงานเปลี่ยน** ระหว่างจอแสดงกับตอนกดยืนยันปิดกะ ไม่ใช่แค่เงินสดที่ควรมี (เช่น มีการขาย/ยกเลิกบิลพร้อมเพย์ระหว่างจอเปิดอยู่ ซึ่งไม่กระทบเงินสด) · กลไก: ฝั่งแอปส่งแฮชของรายงาน X ที่จอแสดง (`shownReportFingerprint` — ครอบ sales/cash/varianceAlertSatang/voids) มาด้วย ปิดกะคำนวณแฮชใหม่จากรายงานสดแล้วเทียบ ไม่ตรงก็ปฏิเสธ ไม่เขียนอะไร · `shownExpectedCashSatang` ยังคงไว้เพื่อข้อความ error ที่อ่านง่าย **หมายเหตุจาก final review (opus), 21 ก.ย. 2026 — ไม่ได้แก้คำตัดสินใจนี้ เพิ่มเพื่อให้ตรงกับที่สร้างจริง (review m-5):** (1) การจัดลำดับ Z ก่อนหน้าสำหรับ `recomputeZChainLenient` จริง ๆ ใช้ลำดับ **insert (`rowid`)** เสมอ ไม่ใช่ "เรียงตาม zNo ที่อ่านได้ ถ้าอ่านไม่ได้ใช้ลำดับแถวเดิม" ตามที่เขียนไว้ข้างบน — เป็นรูลลิ่งของผู้คุมงาน (controller ruling NF-1/NF-3 ระหว่าง Task 6 fix round เพราะ `zNo` ในสแนปช็อตแก้ไขได้ด้วยมือ ส่วน `rowid` ของตาราง append-only ไม่ได้ บันทึกไว้ที่ [บันทึกการทำแผน 3b](../superpowers/plans/2026-09-17-03b-บันทึกการทำแผน3b.md) §3 ข้อ 5) (2) ปุ่มยืนยัน over-drawer (`cash-over-drawer-warning`/`cash-over-drawer-confirm`) ถูกสร้างจริงใน **Task 10** (`CashMoveDialog`) ไม่ใช่ Task 11 ตามที่เขียนไว้ข้างบน | คำตอบ 'ทำได้เลย' 19 ก.ย. |

### ผลที่ตามมาจากรอบที่ 7

- แผน 3b (`docs/superpowers/plans/2026-09-17-03b-pos-shift-close.md`) ปรับตามคำตอบทุกข้อแล้ว: Task 10 เดิมแยกเป็น Task 10 (หน้า X · เงินเข้า-ออก) และ Task 11 (ปิดกะ · เปิดกะด่วน) · Task 11 เดิม (ตรวจ/บันทึก/merge) เป็น Task 12 · ตรวจรันจริงทุก task ใน worktree ชั่วคราวแล้ว
- ยังห้ามขายจริงจนกว่าแผน 3b เสร็จ + รอบทดสอบแท็บเล็ต (D51) + deploy Pages (D48 Q3-2, Q3-17)
