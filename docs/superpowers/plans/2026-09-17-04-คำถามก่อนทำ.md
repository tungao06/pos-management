# แผน 4 (pos-stock) — คำถามถึงเจ้าของก่อนเริ่มทำ

สถานะ: ⏳ **รอเจ้าของตอบ 12 ข้อ (Q4-1 – Q4-12)** · แผน [2026-09-17-04-pos-stock.md](2026-09-17-04-pos-stock.md) เขียนตาม **ค่าแนะนำ** ทุกข้อแล้ว (ตาราง "คำตอบของเจ้าของที่แผนนี้ใช้" ท้ายแผนบอกจุดที่ใช้) · เมื่อตอบแล้วให้บันทึกเป็น **D56** ใน [บันทึกการตัดสินใจ](../../design/00-บันทึกการตัดสินใจ.md) บน `plan-4-draft` ก่อนเริ่ม Task 1 · ถ้าคำตอบข้อใดต่างจากค่าแนะนำ ให้แก้จุดในตารางนั้นก่อนทำ task ที่เกี่ยวข้อง

กติกา: เรื่องที่ spec และ D1–D55 ตัดสินแล้ว **ไม่ถามซ้ำ** (ดูหัวข้อ "ตัดสินใจเชิงเทคนิค" และ "ไม่ถาม เพราะตัดสินแล้ว" ท้ายไฟล์) · แต่ละข้อมีตัวเลือก + **ค่าแนะนำ** + เหตุผล · คำถามใหม่ที่เกิดระหว่างทำให้เพิ่มท้ายไฟล์เป็น Q4-13 เป็นต้นไป แล้วหยุดรอคำตอบก่อนทำ task ที่เกี่ยวข้อง

---

## ก. วันทำการของงานสต็อก

| # | คำถาม | ตัวเลือก | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|---|
| Q4-1 | **งานสต็อก (รับของ · ทำเบส · นับ · เบิกออก) ต้องเปิดกะก่อนไหม และลงวันทำการไหน** — spec §7 invariant 7 เขียนว่า "ไม่มีธุรกรรม business_date ที่ไม่มี shift" แต่ในร้านจริงอาจต้มเบสก่อนเปิดร้าน รับของวันหยุด หรือนับของหลังปิดกะ | (ก) **ไม่ต้องเปิดกะ**: วันทำการ = วันของกะที่เปิดอยู่ ถ้าไม่มีกะ = วันที่ไทยตอนนั้น · invariant 7 ของแผน 8 ใช้กับบิล/เงินสด/กะเท่านั้น · ยกเว้น "จ่ายค่าของจากลิ้นชัก" (Q4-6) ต้องมีกะ · (ข) ต้องเปิดกะก่อนทุกครั้ง (invariant 7 ครอบทุกตาราง) · (ค) ไม่ต้องเปิดกะ และใช้วันที่ไทยตามนาฬิกาเสมอ (ไม่ยึดวันของกะ) | **(ก)** | ทำงานนอกเวลาขายได้โดยไม่ต้องเปิดกะหลอก ๆ (ซึ่งทำให้ยอดเงินของวันเพี้ยน) · ระหว่างกะเปิด ของที่ทำหลังเที่ยงคืนยังเป็นวันเดิมเหมือนการขาย (spec §4.7) · เงินที่ออกจากลิ้นชักยังผูกกับกะเสมอ | Task 3 (`stockBusinessDate` + เทสต์) · Task 4–7 · Task 13 (ส่งต่อ invariant 7 ให้แผน 8) | ⏳ |

## ข. นับสต็อก

| # | คำถาม | ตัวเลือก | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|---|
| Q4-2 | **"ชุดนับหลัก" ~10 รายการที่นับทุกสัปดาห์ คืออะไรบ้าง** — D20 บอกว่า "ของดิบมูลค่าสูง ~10 รายการ" และ spec §5 มีปุ่ม "ของดิบหลัก 10 รายการ / ทั้งหมด" แต่ไม่ได้ระบุรายการ | (ก) มัทฉะ 2 (RM-MAT-01, RM-MAT-02) · ชา 4 (RM-TEA-01 ชาไทยผง, -02 ใบชาใต้, -03 ชาเขียวผง, -04 ใบชาเขียว) · นม 3 (RM-MLK-01 นมสด, -02 นมข้นจืด, -03 นมข้นหวาน) · ครีมชีส RM-CRM-01 · (ข) (ก) + แก้ว 3 ขนาด (PK-CUP-01/02/03) = 13 รายการ · (ค) เจ้าของระบุเอง | **(ก)** | ราคาต่อหน่วยสูงสุด (มัทฉะ ฿4.4–9.6/g) และใช้มากที่สุด (ชา/นมอยู่ในเกือบทุกแก้ว) → ส่วนต่างเป็นเงินมากสุด · แก้วนับง่ายแต่ถูก · ปุ่ม "ทั้งหมด" ยังนับได้ทุก 38 รายการ · รายการเป็นค่าคงที่บรรทัดเดียว แก้ง่าย (แผน 5 ย้ายไปตั้งค่าได้) | Task 3 (`KEY_COUNT_ITEM_CODES`) · Task 12 (`count-scope-key` + e2e) | ⏳ |
| Q4-3 | **ตอนนับ ให้เห็นยอดตามบัญชี (ที่ระบบคิดว่ามี) ก่อนนับหรือไม่** | (ก) **ไม่เห็นจนกว่าจะบันทึกรายการนั้น** แล้วเห็นยอดบัญชีและส่วนต่างทันที (กด "นับใหม่" ได้ก่อนปิดใบนับ) · (ข) เห็นยอดบัญชีตั้งแต่ก่อนนับ · (ค) ไม่เห็นเลยจนปิดใบนับ | **(ก)** | หลักเดียวกับการนับเงินปิดกะ (Q3b-3 · D52): ถ้าเห็นตัวเลขก่อน มักนับให้ "ตรง" · เห็นส่วนต่างทีละรายการหลังบันทึกช่วยจับการนับผิด (เช่น ใส่เป็นกรัมแทนถุง) ได้ทันทีก่อนปรับยอดจริง | Task 12 (`CountRow` · `CountScreen.test.tsx` · e2e) | ⏳ |
| Q4-4 | **ส่วนต่างการนับมาก ต้องใส่เหตุผลหรือ PIN เจ้าของไหม** | (ก) **ไม่ต้อง** — แสดงส่วนต่างเป็นกรัม/มล. และบาท ต่อรายการและรวม · ทุกคนปิดใบนับได้ (spec §5) · (ข) ส่วนต่างรวมเกิน ฿X ต้องใส่เหตุผลก่อนปิดใบนับ (ต้องกำหนด X) · (ค) ต้อง PIN เจ้าของปิดใบนับ | **(ก)** | D9: การนับคือความจริงของต้นทุน — ยิ่งปิดใบนับง่ายยิ่งนับบ่อย · ร้าน 2 คนเป็นเจ้าของทั้งคู่ · ใบนับเก็บส่วนต่างเป็นบาทไว้ถาวรให้รายงานส่วนต่างรายสัปดาห์ของแผน 7 | Task 7 (`closeStockCount`) · Task 12 | ⏳ |

## ค. รับของเข้า

| # | คำถาม | ตัวเลือก | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|---|
| Q4-5 | **ถ่ายรูปใบเสร็จ** (spec §5, D19) ทำในแผนนี้หรือไม่ — รูปต้องเก็บใน OPFS ก่อนอัปโหลดตอน sync (แผน 5) และไฟล์สำรองของแผน 3b สำรองแค่ฐานข้อมูล ไม่รวมรูป | (ก) **เลื่อนไปแผน 5** (มีที่อัปโหลดและสำรองรูป) — แผน 4 ไม่มีปุ่มถ่ายรูป `receipt_image_ref` เป็นค่าว่าง · (ข) ทำในแผน 4: ถ่ายรูปเก็บใน OPFS (หายพร้อมแท็บเล็ตจนกว่าแผน 5 จะอัปโหลด) · (ค) ไม่เก็บรูปเลย | **(ก)** | รูปที่อยู่แค่ในแท็บเล็ตไม่ได้สำรองที่ไหน · ยังไม่ขายจริงจนจบทุกแผน (D51) จึงไม่เสียอะไร · ลดงานทดสอบกล้องบนแท็บเล็ต | Task 4 (`receiptImageRef: null`) · Task 13 (ส่งต่อแผน 5) | ⏳ |
| Q4-6 | **จ่ายค่าของด้วยเงินในลิ้นชัก** — ถ้าไม่บันทึก "จ่ายออก" ด้วย เงินปิดกะจะขาด | (ก) **ช่องติ๊ก "จ่ายด้วยเงินในลิ้นชัก"** บนหน้ารับของ (ค่าเริ่มต้นไม่ติ๊ก) → บันทึก `PAID_OUT` เท่ายอดใบรับของในธุรกรรมเดียวกัน (ต้องมีกะเปิด) · (ข) ไม่เชื่อม — ไปกด "เงินเข้า-ออก" บนหน้าขายเองอีกครั้ง · (ค) บังคับทุกใบรับของ | **(ก)** | กันลืมและกันบันทึกซ้ำ ยอดตรงกันเสมอ · ของที่โอน/จ่ายเงินส่วนตัวไม่ต้องติ๊ก · เข้า X/Z ของกะเหมือนจ่ายออกปกติ (Q3b-9 · D52) | Task 4 (`paidFromDrawer`) · Task 9 (`receive-paid-drawer` + e2e) | ⏳ |
| Q4-7 | **บันทึกรับของผิดแล้ว แก้อย่างไร** — ตารางบัญชีแก้/ลบไม่ได้ และต้นทุนเฉลี่ยคิดตอนของเข้า การ "ยกเลิก" ด้วย movement กลับทิศจะไม่ย้อนต้นทุนเฉลี่ยที่ถูกดึงไปแล้ว | (ก) **แผน 4 ไม่มีปุ่มยกเลิกใบรับของ**: ก่อนบันทึกเห็นทุกบรรทัด + ยอดรวม ลบบรรทัดได้ · เตือนและต้องยืนยันเมื่อราคาต่างจากต้นทุนเดิมเกิน 10% (D47 ข้อ 3) · จำนวนผิดแก้ด้วยการนับ · เครื่องมือแก้ใบรับของ (กลับรายการ + คิดต้นทุนใหม่จากประวัติ) ทำในแผน 5 · (ข) ปุ่มยกเลิกใบรับของภายในวันเดียวกัน (movement กลับทิศ + เหตุผล + PIN เจ้าของ) — ต้นทุนเฉลี่ยไม่ย้อน · (ค) (ข) + คำนวณต้นทุนเฉลี่ยใหม่ทั้งประวัติของสินค้านั้น (งานใหญ่ ต้องออกแบบใหม่) | **(ก)** | กันความผิดพลาดที่ต้นทาง (ตรวจก่อนบันทึก + เตือนราคา) ได้ผลกว่าแก้ทีหลัง · (ข) ให้ภาพว่าแก้แล้วทั้งที่ต้นทุนยังเพี้ยน · (ค) ใหญ่เกินแผนนี้ | Task 4 (ไม่มี API ยกเลิก) · Task 9 (`receive-remove-<i>`, `receive-jump-<i>`) · Task 13 (ส่งต่อแผน 5) | ⏳ |

## ง. ปรับสต็อกและทำเบส

| # | คำถาม | ตัวเลือก | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|---|
| Q4-8 | **แจก/ชดเชยเครื่องดื่ม และ "อื่น ๆ" บันทึกเป็นอะไร ต้อง PIN ไหม** — D50 Q3-20 ให้แผน 4 ทำ "เบิกของออกพร้อมเหตุผล" แต่ D39 ล็อกประเภท movement ไว้ 11 ค่า (ไม่มี "แจก") | (ก) หัวใบเบิกของ (`stock_adjustment` — ตารางใหม่ ดู T4-1) เก็บประเภท 5 แบบ: ของเสีย · หมดอายุ · ทดลองสูตร · **แจก/ชดเชย** · อื่น ๆ + เหตุผลบังคับ · movement: ของเสีย/แจก/อื่นๆ = `WASTE`, หมดอายุ = `EXPIRED`, ทดลอง = `TRIAL` (11 ค่าคงเดิม) · เบิกได้ทั้ง "เป็นวัตถุดิบ" และ "เป็นแก้วตามสูตร" (ตัดทุกวัตถุดิบเหมือนขาย) · ทุกคนทำได้ **ไม่ต้อง PIN** · (ข) เหมือน (ก) แต่แจก/ชดเชยต้อง PIN เจ้าของ · (ค) เพิ่มประเภท movement ใหม่ `GIVEAWAY` (แก้ D39 · contracts) | **(ก)** | รายงานแยก "แจก" ออกจาก "ของเสีย" ได้จากหัวใบ โดยไม่แตะบัญชี 11 ค่า · เบิกเป็นแก้วตรงกับที่ร้านทำจริง (แจกทั้งแก้ว ไม่ใช่ทีละวัตถุดิบ) · ร้าน 2 คน (spec §5: ปรับสต็อก = ทุกคน) | Task 1 (`adjustMovementKind`) · Task 2 (`AdjustReason`) · Task 6 · Task 11 | ⏳ |
| Q4-9 | **ทำเบสใหม่ตอนที่เบสเดิมยังเหลือ** — spec §4.6 ให้เบสเป็นก้อนเดียว ไม่ติดตามอายุต่อ batch | (ก) หน้าทำเบสแสดงของเหลือ + เวลาหมดอายุ · ถ้าหมดอายุแล้วมีปุ่ม **"ทิ้งของเก่าก่อน"** (ไม่บังคับ) · ปุ่ม "ทิ้ง" บนหน้าสต็อกทิ้งทั้งก้อน (หมดอายุแล้ว → `EXPIRED` · ยังไม่หมด → `WASTE`) · อายุที่แสดง = batch ล่าสุด · (ข) บังคับทิ้งของเก่าก่อนทำใหม่ทุกครั้ง · (ค) ติดตามอายุต่อ batch (FIFO — spec บอกไม่ทำในเฟส 1) | **(ก)** | เตือนให้เห็นโดยไม่ขวางงานเร่งตอนเปิดร้าน · ร้านที่เทใหม่ทับของเก่าจะเห็นอายุยาวกว่าจริง — ยอมรับตาม spec §4.6 | Task 6 (`discardBase`) · Task 8 (`base-discard-*`) · Task 10 (`produce-leftover`, `produce-discard-first`) | ⏳ |

## จ. เตือน

| # | คำถาม | ตัวเลือก | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|---|
| Q4-10 | **แสดงคำเตือนสต็อกที่ไหน** (spec §1.1 "เตือนใกล้หมด" · §5 หน้าสต็อก · D28 เบสติดลบเตือนแดง) | (ก) ปุ่ม **"สต็อก ⚠ N"** บนหน้าขาย (N = ของดิบใกล้หมด/หมด/ติดลบ + เบสติดลบ/หมดอายุ) + **แถบแดง "เบสหมดอายุ: …"** บนหน้าขาย · "ใกล้หมดอายุ" (เหลือ ≤ 60 นาที) แสดงเฉพาะหน้าสต็อก · ไม่มี popup ไม่บล็อกการขาย · (ข) เฉพาะหน้าสต็อก · (ค) popup ตอนเปิดกะ | **(ก)** | มัทฉะช็อตอายุ 4 ชม. ครีมชีสโฟม 12 ชม. — ต้องเห็นจากหน้าขายโดยไม่ต้องเข้าเมนู · ไม่บล็อกการขาย (spec §4.2) | Task 1 (`EXPIRY_SOON_MINUTES`) · Task 3 (`alert`, `expiredBaseCodes`) · Task 8 (`nav-stock`, `base-expired`) | ⏳ |
| Q4-11 | **เบสใกล้หมด** — จุดสั่งซื้อของเบสในไฟล์ร้านเป็น 0 ทุกตัว | (ก) เบส **ไม่มีสถานะ "ใกล้หมด"** ในแผน 4 — เตือนเฉพาะติดลบหรือหมดอายุ (เบสที่หมด = 0 แสดง "หมด" แต่ไม่นับเป็นคำเตือน เพราะเช้าทุกวันจะหมดทุกตัว) · ตั้งจุดเตือนต่อเบสได้ในหน้าจัดการสินค้า (แผน 5) · (ข) เตือนเมื่อเบสเหลือน้อยกว่า N แก้ว (ต้องกำหนด N ต่อเบส) · (ค) เบสที่หมดก็นับเป็นคำเตือน | **(ก)** | ไม่ให้ปุ่มเตือนแดงตลอดเวลาจนไม่มีใครดู · เบสติดลบคือสัญญาณ "ลืมบันทึกทำเบส" ที่ D28 ต้องการ | Task 3 (`alert` ของ prepared + เทสต์ `alertCount: 31`) | ⏳ |
| Q4-12 | **เตือนให้นับสต็อก** (D20 นับสัปดาห์ละครั้ง) | (ก) หน้าสต็อกแสดง "นับสต็อกล่าสุด …" + **แถบแดงเมื่อไม่ได้นับ 7 วัน** (หรือยังไม่เคยนับ) · ใบนับที่ไม่มีรายการไม่นับว่า "นับแล้ว" · (ข) ไม่เตือน · (ค) เตือนบนหน้าขายด้วย | **(ก)** | เตือนตรงที่คนจะไปนับ ไม่รกหน้าขาย · ครั้งแรกขึ้นเตือนทันที = ชวนนับยอดยกมา (D30) | Task 3 (`COUNT_DUE_DAYS`, `countDue`) · Task 7 · Task 8 (`stock-count-due`) · Task 12 e2e | ⏳ |

---

## ตัดสินใจเชิงเทคนิค (ไม่ต้องตอบ — ตามมาจาก spec/D หรือเป็นเรื่องภายในโค้ด · คัดค้านได้)

| # | เรื่อง | ตัดสินใจ | เหตุผล/ที่มา |
|---|---|---|---|
| T4-1 | **เก็บเหตุผลของการปรับสต็อกที่ไหน** | ตารางใหม่ `stock_adjustment` (T — id · business_date · reason_code · reason · detail_json · device_id · created_by · created_at) ทั้ง sqlite และ pg สร้างด้วย `drizzle-kit generate` · movement อ้าง `ref_type = 'stock_adjustment'` · ไม่มี trigger (ไม่ใช่ตารางบัญชี เหมือน `purchase`) · **เป็นการเพิ่มตารางที่ spec §3.3 ยังไม่มี — เจ้าของโปรดรับทราบ** แก้ spec ในรอบเอกสารถัดไป | spec §5 ต้องมีประเภท + เหตุผล แต่ `stock_movement` ไม่มีคอลัมน์เหตุผล · ทางเลือก "เพิ่มคอลัมน์ `reason` ใน `stock_movement`" ซ้ำเหตุผลทุกแถวและแยก "แจก" ไม่ได้ · `audit_log` เป็น local-only ไม่ sync |
| T4-2 | expected ของใบนับ แช่แข็งเมื่อไร | **ต่อบรรทัด ตอนบันทึกรายการนั้น** (= ยอดบัญชี ณ เวลานับ) · ปิดใบนับใช้ส่วนต่าง = นับได้ − expected ที่แช่แข็ง | spec §3.3 "expected = ยอดตามบัญชี ณ เวลานับ" ขัดกับ §4.5 "ณ เวลาเปิดใบนับ" — เลือก §3.3 เพราะถ้าขายระหว่างนับ แบบ §4.5 จะหักของที่ขายซ้ำ (เทสต์: นับนม 405 ml แล้วขายอีก 46 ml → เหลือ 359 ml ถูกต้อง) |
| T4-3 | ยอดยกมา (D30) | รายการที่ **ไม่เคยอยู่ในใบนับที่ปิดแล้ว** → movement `OPENING` = นับได้ − ยอดบัญชี ที่ราคามาตรฐาน · ครั้งต่อไป `COUNT_ADJ` ที่ราคาเฉลี่ย | D30 "นับจริงครั้งแรก → OPENING ที่ราคาตั้งต้น" · ราคาตั้งต้น = standard cost (D34) · ใช้ได้กับสินค้าที่เพิ่มภายหลังด้วย |
| T4-4 | ของไม่นับสต็อกใน `item_cost_state` (งานส่งต่อแผน 3 I-2b M-4) | `insertMovements` ยังเขียนแคชทุก item (invariant 5 rebuild แล้วตรง) · หน้าสต็อก/รับ/นับ/เบิกเป็นรายการ **ข้าม** item ที่ `is_tracked = false` และชุดบรรจุภัณฑ์ | D29 · ไม่แก้โค้ดขายของแผน 3 |
| T4-5 | มูลค่าสต็อก | `costSatang(คงเหลือ, ราคาเฉลี่ย)` เมื่อคงเหลือ > 0 · ติดลบหรือ 0 = ฿0 | spec §5 "มูลค่า" · ของติดลบไม่ใช่ทรัพย์สิน |
| T4-6 | สถานะ | ติดลบ < 0 · หมด = 0 · ใกล้หมด = ไม่เกินจุดสั่งซื้อ (เมื่อจุดสั่งซื้อ > 0) · ปกติ | spec §5 · จุดสั่งซื้อจากไฟล์ร้าน (§3.3 `reorder_point_milli`) |
| T4-7 | อายุเบส | ใช้ `expires_at` ของ batch ล่าสุด (ลำดับ insert/rowid ไม่ใช่นาฬิกา) · หมดอายุเมื่อถึงเวลาและยังมีของ | spec §4.6 (ไม่ทำ FIFO) · 3b ruling ใช้ rowid แทนเวลา |
| T4-8 | ป้าย "ยังไม่ส่ง N รายการ" | ใบรับของ / batch / ใบนับ / ใบเบิก นับ **ใบละ 1** รวมบรรทัดและ movement (movement นับใต้ `refType:refId`) | ต่อยอด D50 Q3-26 (นับบิล ไม่นับแถว) — ถ้านับรายแถว รับของ 5 รายการจะขึ้น 11 |
| T4-9 | รับของ | เฉพาะของดิบที่นับสต็อก · ยอด ≥ 0 (ของแถมได้) · ต้นทุนต่อหน่วยใช้ = ยอด × 10⁹ / ปริมาณ (usat) · เทียบกับต้นทุนปัจจุบัน (ราคาเฉลี่ย หรือมาตรฐานก่อนมีของเข้า) · ต่างเกิน 10% → `PRICE_JUMP` ต้องยืนยัน | D47 ข้อ 3 · D34 · เบสได้จากการทำเบส ไม่ได้ซื้อ |
| T4-10 | ทิศทางการปรับ | ปรับสต็อก = **เบิกออกอย่างเดียว** · เพิ่มยอดทำผ่านการนับ (`COUNT_ADJ`) | ตรงกับรายการใน spec §5 (ของเสีย/หมดอายุ/ทดลอง/อื่น ๆ) |
| T4-11 | บรรทัดร่างของใบนับ | บันทึก/นับใหม่/ลบได้ระหว่างใบนับเปิด (upsert/DELETE `stock_count_line`) · **ยังไม่เข้า outbox** จนปิดใบนับ · ใบนับเปิดได้ครั้งละใบต่อเครื่อง เปิดแอปใหม่แล้วนับต่อได้ | ไม่ใช่ตารางบัญชี (ไม่มี trigger) และไม่เคยถูกส่งก่อนปิด · ล็อกจอ 10 นาทีไม่ทำให้ของที่นับหาย |
| T4-12 | ขอบเขตตัวเลข | batch 0.1×–5× (`scale_bp` 1,000–50,000) · yield ไม่เกิน 2 เท่าของมาตรฐาน · จำนวนที่พิมพ์ ≤ 9,999.999 หน่วย ทศนิยม 3 ตำแหน่ง · ยอดต่อบรรทัด ≤ ฿100,000 · ≤ 50 บรรทัดต่อใบ · แจกไม่เกิน 99 แก้วต่อบรรทัด | กันพิมพ์ผิด (หลักเดียวกับ `MAX_CASH_MOVEMENT_SATANG` ของ 3b) |
| T4-13 | error ใหม่ | `PRICE_JUMP` (detail = รหัสสินค้า) · `STOCK_COUNT_NOT_OPEN` · ที่เหลือใช้ `BAD_INPUT`/`NO_OPEN_SHIFT`/`NO_RECIPE` เดิม | ข้อความไทยใน `ui/errors.ts` |
| T4-14 | spec §6.1 "แผน 4 เลือก" (pull ตาม `server_seq`) | ไม่ใช่งานของแผนนี้ — ตอนเขียน spec แผน 4 คือ API · ส่งต่อแผน 5 (api-sync) ในบันทึกการทำแผน | README: แผน 4 = สต็อก, แผน 5 = api-sync |

## ไม่ถาม เพราะตัดสินแล้ว

- ใครทำงานสต็อกได้ → ทุกคน (spec §5) · ไม่ต้อง PIN
- นับสัปดาห์ละครั้ง นับเป็นหน่วยซื้อ (D20) · ยอดยกมานับครั้งแรกทั้งหมด → OPENING ที่ราคาตั้งต้น (D30, D35)
- ของไม่นับสต็อก: น้ำแข็ง น้ำ น้ำดื่มถัง เกลือ (D29) · เบสติดลบไม่บล็อกการขาย (D28, spec §4.2)
- ต้นทุนถัวเฉลี่ยเคลื่อนที่ · ก่อนมีของเข้าใช้ต้นทุนมาตรฐาน (spec §4.4, D19, D34)
- เตือนราคารับเข้าต่างเกิน 10% (D47 ข้อ 3) · จุดสั่งซื้อมาจากไฟล์ร้าน (spec §3.3, §9)
- ปุ่ม "ทิ้ง" เบส = EXPIRED ทั้งก้อน ไม่มี FIFO (spec §4.6) · แจก/ชดเชยไม่ใช่บิล 0 บาท (D50 Q3-20)
