# แผน 3b (pos-shift-close) — คำถามถึงเจ้าของก่อนเริ่มทำ

สถานะ: 🟢 **ตอบครบ 18 ข้อแล้ว** (19 ก.ย. "เห็นด้วย ทำต่อได้เลย" / "ทำได้เลย" = ใช้ค่าแนะนำทุกข้อ · 21 ก.ย. "ทำได้เลย") · Q3b-1 – Q3b-10 → **D52** · Q3b-11 – Q3b-13 (เพิ่มจาก review แผน) → **D53** · Q3b-14 – Q3b-17 (เพิ่มจาก review Task 6 รอบ fix 1b) → **D54** · Q3b-18 (เพิ่มจาก final review known risk (h) / finding m-1) → **D55** ใน [บันทึกการตัดสินใจ](../../design/00-บันทึกการตัดสินใจ.md) · แผน [2026-09-17-03b-pos-shift-close.md](2026-09-17-03b-pos-shift-close.md) ปรับตามคำตอบแล้ว (ตาราง "คำตอบของเจ้าของที่แผนนี้ใช้" ท้ายแผนบอกจุดที่ใช้) · ถ้าเจ้าของเปลี่ยนคำตอบภายหลัง ให้แก้จุดในตารางนั้นก่อนเริ่ม task ที่เกี่ยวข้อง

กติกา: เรื่องที่ spec และ D1–D53 ตัดสินแล้ว **ไม่ถามซ้ำ** (ดูหัวข้อ "ตัดสินใจเชิงเทคนิค" ท้ายไฟล์) · แต่ละข้อมีตัวเลือก + **ค่าแนะนำ** + เหตุผลบรรทัดเดียว · คำถามใหม่ที่เกิดระหว่างทำให้เพิ่มท้ายไฟล์เป็น Q3b-14 เป็นต้นไป แล้วหยุดรอคำตอบก่อนทำ task ที่เกี่ยวข้อง

---

## ก. ปิดกะและนับเงิน

| # | คำถาม | ตัวเลือก | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|---|
| Q3b-1 | นับเงินปิดกะ นับธนบัตร/เหรียญชนิดไหนบ้าง — spec §5 เขียนตาราง 1000/500/100/50/20/10/5/2/1 แต่ไม่ได้พูดถึงเหรียญสตางค์ (ส่วนลดใส่เศษสตางค์ได้ เช่น 45.50) | (ก) 9 ชนิดตาม spec เท่านั้น · (ข) 9 ชนิด + เหรียญ 50 สตางค์ + 25 สตางค์ · (ค) 9 ชนิด + ช่อง "เศษ/อื่น ๆ" ใส่เป็นบาท | **(ก)** | ราคาเมนูเป็นบาทเต็มทั้งหมด · ส่วนลดมีเศษสตางค์น้อยมาก และส่วนต่างไม่ถึง 1 บาทต่ำกว่าเกณฑ์ ฿20 อยู่แล้ว · เพิ่มชนิดภายหลังแก้ค่าคงที่ตัวเดียว | Task 1 (`CASH_DENOMINATIONS_SATANG`) · Task 11 (`CloseShiftScreen`) | ✅ (ก) — D52 |
| Q3b-2 | ใครปิดกะได้ และยืนยันอย่างไร — spec §5 เขียนว่า "ปิดวันต้อง owner" แต่ไม่ได้บอกวิธียืนยัน | (ก) owner ที่ล็อกอินอยู่กดได้เลย · (ข) คนที่ล็อกอินนับเงิน แล้ว **เจ้าของกรอก PIN ซ้ำ** ทุกครั้ง (เลือกตัวเองได้ เหมือน void — D50 Q3-22) · (ค) ต้องใช้ PIN ของเจ้าของอีกคนเท่านั้น | **(ข)** | ใช้กลไกเดียวกับ void (ตัวนับ PIN ผิด D50 Q3-21) · ถ้ามีพนักงานในอนาคตก็นับเองได้ แต่เจ้าของต้องยืนยัน · Z บันทึกทั้ง `countedBy` และ `closedBy` | Task 6 (`closeShift` → `requireOwnerPin`) · Task 11 (`CloseShiftScreen` เลือกเจ้าของ + PinPad) | ✅ (ข) — D52 |
| Q3b-3 | ตอนนับเงินปิดกะ ให้เห็น "เงินสดที่ควรมี" ก่อนนับหรือไม่ (นับแบบปิดตา — blind count) · *แก้ถ้อยคำตาม review I-2: ร่างแรกมีทางเข้าหน้าปิดกะทางเดียวคือผ่านหน้า X ซึ่งแสดงยอดที่ควรมี การนับจึงไม่ปิดตาจริง* | (ก) นับก่อนแล้วจึงเห็นยอด **และ** หน้า X ซ่อนบรรทัด "เงินสดที่ควรมี" ระหว่างกะเปิด (ปุ่ม "ปิดกะ" ย้ายไปแถบบนของหน้าขาย) · (ข) นับก่อนแล้วจึงเห็นยอด แต่หน้า X ยังแสดงยอดที่ควรมี (ปุ่มปิดกะอยู่หน้า X — การนับไม่ปิดตาจริง เพราะเห็นยอดก่อนกดปิดกะ) · (ค) เห็นยอดตั้งแต่เริ่มนับ | **(ก)** | กันการนับให้ "ตรง" ตามตัวเลขที่เห็น ทำให้ส่วนต่างสะท้อนความจริง · รายงาน X ยังเปิดดูได้ตลอดตาม spec §4.8 (ยอดขาย เงินเข้า-ออกครบ) แต่เป็นคนละทางกับการปิดกะ · ยอดที่ควรมีและส่วนต่างแสดงหลังกด "นับเสร็จ" (แก้จำนวนที่นับได้ก่อนยืนยัน) | Task 9 (`DrawerTable hideExpected`) · Task 10 (`ShiftScreen` ไม่มียอดที่ควรมี/ปุ่มปิดกะ · e2e `shift-x`) · Task 11 (`close-shift-open` บนหน้าขาย · ขั้น `counted` · e2e ตรวจ `close-expected` ไม่แสดงก่อนนับ) | ✅ (ก) — D52 |
| Q3b-4 | ยอดขายสุทธิ (net) และยอดสะสม grand total ใน Z **นับบิลที่ยกเลิกหรือไม่** — spec §4.3 บอกว่าบิลยกเลิก "อยู่ใน gross และแยกเป็นยอดยกเลิก" แต่ §4.8 บอกแค่ "grand = Z ก่อนหน้า + ยอดกะนี้" | (ก) net = gross − ส่วนลด − ยอดบิลยกเลิก (= ยอดบิลที่ยังชำระอยู่) · grand total สะสม net · (ข) net = gross − ส่วนลด (รวมบิลยกเลิก) · grand total สะสมตัวนี้ | **(ก)** | net ต้องเท่ากับเงินที่ร้านได้จริงจากการขาย · grand total ใช้เทียบเกณฑ์ VAT 1.8 ล้าน (D8) จึงไม่ควรนับบิลที่คืนเงินไปแล้ว · gross และยอดยกเลิกยังแสดงแยกครบ | Task 1 (`summarizeShiftSales`, `assertSalesSummary`) · Task 6 (Z) | ✅ (ก) — D52 |
| Q3b-5 | รายงาน Z แสดงหรือส่งออกอย่างไร (ยังไม่มีเครื่องพิมพ์ — D4) | (ก) แสดงบนจอทันทีหลังปิดกะ + ดู Z ย้อนหลังทุกใบในเครื่องได้ · ไม่ส่งออกแยก · (ข) (ก) + ปุ่มบันทึก Z เป็นไฟล์ JSON/CSV · (ค) (ก) + แชร์เป็นรูปภาพ | **(ก)** | Z อยู่ในไฟล์สำรองฐานข้อมูลอยู่แล้ว (Q3b-6) · export CSV เป็นงานรายงานของแผน 7 · ลดงานที่ต้องทดสอบบนแท็บเล็ต | Task 6 (`listZReports`, `getZReport`) · Task 9 (`ZReportScreen`, `ZListScreen`) | ✅ (ก) — D52 |

## ข. สำรองไฟล์ฐานข้อมูล

| # | คำถาม | ตัวเลือก | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|---|
| Q3b-6 | ปุ่มสำรองไฟล์: ส่งไฟล์ไปที่ไหน รูปแบบอะไร | (ก) ไฟล์ SQLite ทั้งไฟล์ (`pool.exportFile` — ผล spike I3) **ดาวน์โหลดลงโฟลเดอร์ Downloads** ชื่อ `dayo-pos-A-YYYYMMDD-HHmmss.sqlite3` แล้วเจ้าของย้ายไป Google Drive/คอมเอง · (ข) (ก) + ปุ่ม "แชร์" (Web Share) ส่งเข้า Drive/LINE ตรง ๆ · (ค) รอแผน 5 (สำรองขึ้นเซิร์ฟเวอร์) ไม่ทำปุ่มในเครื่อง | **(ก)** | ใช้ได้ทันทีแบบออฟไลน์ ไม่ต้องมีบัญชีใด · Chrome อนุญาตแชร์ไฟล์เฉพาะบางนามสกุล และ `.sqlite3` น่าจะไม่อยู่ในรายการ (ต้องลองบนแท็บเล็ตจริงถ้าเลือก (ข)) · (ค) ขัด D48 Q3-17 ที่ต้องมีสำรองก่อนขายจริง | Task 7 (`exportBackup`, `backupFileName`) · Task 8 (`BackupScreen`, `downloadBytes`) | ✅ (ก) — D52 |
| Q3b-7 | สำรองบ่อยแค่ไหน และบังคับหรือไม่ — spec §11 เขียนว่า "export ไฟล์ SQLite ทุกคืนชั่วคราว" · *เพิ่มตาม review I-4: แถบเตือนหายตอนไหน* | (ก) หลังปิดกะ หน้า Z ชวนสำรอง + **แถบเตือนสีแดง** ที่หน้า Z และหน้าเปิดกะ จนกว่าจะสำรองหลัง Z ใบล่าสุด · แถบหายเมื่อเจ้าของกด **"บันทึกไฟล์แล้ว"** หลังเห็นไฟล์ในโฟลเดอร์ดาวน์โหลด (ไม่ใช่ตอนกดดาวน์โหลด) · ไม่บล็อกการเปิดกะ · (ข) บังคับสำรองก่อนเปิดกะใหม่ได้ · (ค) มีแค่ปุ่ม ไม่เตือน | **(ก)** | เตือนชัดทุกวันโดยไม่ทำให้เปิดร้านไม่ได้ ถ้าเครื่องมีปัญหาเรื่องดาวน์โหลด · เบราว์เซอร์ไม่บอกว่าไฟล์ลงเครื่องสำเร็จ จึงต้องให้คนยืนยัน · ถ้า S9 (persistent storage) ไม่ผ่านบนแท็บเล็ต (F4) ค่อยเปลี่ยนเป็น (ข) | Task 7 (`isBackupDue`, `confirmBackupSaved`) · Task 8 (`backup-confirm`) · Task 9 (`ZReportScreen`) · Task 11 (`OpenShiftScreen`) | ✅ (ก) — D52 |

## ค. เรื่องอื่นในกะ

| # | คำถาม | ตัวเลือก | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|---|
| Q3b-8 | ลืมปิดกะข้ามวัน ทำอย่างไร — spec §4.7 ให้ขายหลังเที่ยงคืนยังเป็นวันทำการเดิม และ D22 ให้ 1 กะ = 1 วันทำการ แต่ไม่ได้บอกว่าเมื่อไรถือว่า "ลืมปิด" | (ก) **เตือนแถบแดงบนหน้าขาย** ตั้งแต่ 05:00 ของวันถัดไป (เวลาไทย) ขายต่อได้ · (ข) บังคับปิดกะก่อนขายต่อเมื่อถึง 05:00 · (ค) ไม่ทำอะไร | **(ก) เวลา 05:00** | ไม่ทำให้ขายไม่ได้กลางคัน แต่เห็นชัดตั้งแต่เช้า · ร้านไม่เปิดถึงตี 5 จึงไม่ชนการขายดึกที่ตั้งใจ | Task 7 (`isShiftStale`, `STALE_SHIFT_CUTOFF_HOURS`) · Task 10 (`SellScreen` แถบ `shift-stale`) | ✅ (ก) 05:00 — D52 |
| Q3b-9 | บันทึก **เงินเข้า / จ่ายออก / นำเงินออกไปเก็บ** (`PAID_IN`/`PAID_OUT`/`DROP`) ทำในแผน 3b หรือไม่ — สูตร D36 มีสามตัวนี้ แต่แผน 3 ยังไม่มีหน้าจอบันทึก ถ้าไม่ทำ เช่นหยิบเงินลิ้นชักไปซื้อน้ำแข็ง ปิดกะจะขาดทุกครั้ง | (ก) **ทำใน 3b**: ปุ่ม "เงินเข้า-ออก" บนหน้าขาย · ทุกคนที่ล็อกอินบันทึกได้ · บังคับเหตุผล · (ข) ทำ แต่ `PAID_OUT`/`DROP` ต้อง PIN เจ้าของ · (ค) ไม่ทำ (รอแผน 4/7) | **(ก)** | ปิดกะจะตรงก็ต่อเมื่อบันทึกเงินออกได้ · ร้าน 2 คนเป็นเจ้าของทั้งคู่ · ทุกแถวมีคนทำ + เหตุผล + เข้า X/Z | Task 4 (`recordCashMovement`) · Task 10 (`CashMoveDialog`) | ✅ (ก) — D52 |
| Q3b-10 | **"เปิดกะด่วน"** (spec §4.8: owner เปิดด้วยเงินทอน 0 "พร้อม event"; แผน 3 M18) — ตาราง `order_event` ผูกกับบิลเท่านั้น จะเก็บ "event" ของการเปิดกะด่วนไว้ที่ไหน และยืนยันตัวอย่างไร | (ก) แถว `audit_log` (`entity='shift'`, `action='quick_open'`) ในธุรกรรมเดียวกับการเปิดกะ + ธง `openedQuick` ใน snapshot ของ Z (ซิงก์ไปกับ `z_report` และมีแฮชคุม) · เจ้าของที่ล็อกอินกดได้เลย · **ไม่แก้ schema** · (ข) เพิ่มคอลัมน์ `shift.open_kind` (migration ทั้ง sqlite และ pg) · (ค) (ก) แต่ต้องกรอก PIN เจ้าของซ้ำ | **(ก)** | ไม่ต้องแก้ schema 2 dialect กลางทาง · การเปิดกะด่วนไม่มีเงินเข้าออก ความเสี่ยงต่ำ · `audit_log` เป็น local-only ในแผน 3 (notes §5) แต่ธงใน Z ซิงก์ขึ้นเซิร์ฟเวอร์ได้ · หมายเหตุ: เปิดกะปกติด้วยเงินทอน 0 ทำได้ทุกคนอยู่แล้ว (D48 Q3-12) — เปิดกะด่วนต่างแค่ข้ามการกรอกเงินทอน + มีป้าย/บันทึกแยก | Task 3 (`quickOpenShift`, `wasQuickOpened`) · Task 5/6 (`openedQuick`) · Task 11 (`OpenShiftScreen` ปุ่ม `shift-quick-open`) | ✅ (ก) — D52 |

## ง. คำถามเพิ่มจาก review แผน (19 ก.ย.)

| # | คำถาม | ตัวเลือก | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|---|
| Q3b-11 | ถ้า **Z ใบก่อนหน้าแฮชไม่ตรง** (มีคนแก้ไฟล์ หรือไฟล์ OPFS เสีย) จะทำอย่างไร — ร่างแรก (T3b-6) ปฏิเสธการปิดกะทุกครั้งด้วยข้อความ "ข้อมูลไม่ถูกต้อง" ทำให้ปิดกะไม่ได้อีกเลย เปิดกะใหม่ไม่ได้ และไม่มี Z ใหม่ (review I-3) | (ก) **ปิดกะได้** แต่ใช้รหัสเฉพาะ `Z_CHAIN_BROKEN` · เจ้าของกรอก PIN ซ้ำเพื่อรับทราบ · Z ใบใหม่มีธงเตือนถาวร แสดงที่หน้า Z และ Z ย้อนหลัง · grand total ต่อจากค่าที่ **คำนวณใหม่** จาก snapshot ทุกใบเรียงตาม `zNo` · เขียน `audit_log` · (ข) บล็อกการปิดกะจนกว่าผู้ดูแลระบบจะแก้ · (ค) ปิดกะได้ ต่อจากค่าที่เก็บไว้ + ธงเตือน | **(ก)** | ร้านต้องปิดวันได้เสมอ · ค่าที่เก็บใน Z ที่แฮชไม่ตรงเชื่อไม่ได้ จึงคำนวณใหม่จากทุกใบ · ธงถาวรใน Z (มีแฮชคุม) + audit ทำให้ตรวจย้อนหลังได้ว่าเกิดอะไรและใครรับทราบ | Task 1 (`ZChainWarning`, `recomputeZChain`) · Task 6 (`Z_CHAIN_BROKEN`, `acknowledgeZChainBroken`) · Task 9 (`z-chain-warning`, `z-warn-<i>`) · Task 11 (`close-chain-broken`) | ✅ (ก) — D53 |
| Q3b-12 | **ปิดวันเทียบยอดพร้อมเพย์กับแอปธนาคาร** — spec §12 เขียนว่า "ในระหว่างนี้ปิดวันเทียบยอด QR กับแอปธนาคาร" แต่ร่างแรกแสดงแค่ยอดรับพร้อมเพย์ ซึ่งรวมบิลที่ยกเลิกและโอนคืนแล้ว | (ก) แสดงพร้อมเพย์ "รับ / โอนคืน / สุทธิ" ที่ X และ Z · (ข) (ก) + ช่องกรอกยอดจากแอปธนาคารตอนปิดกะ แล้วแช่แข็งส่วนต่าง (ธนาคาร − สุทธิ) ใน Z · (ค) ไม่ทำ | **(ข)** · ช่องกรอก **ไม่บังคับ ไม่มีเกณฑ์/เหตุผล** | จับ QR ที่ "ยืนยันด้วยตา" ผิดได้ทุกวัน (ความเสี่ยงใน spec §12) · ไม่บังคับเพื่อไม่ให้ปิดกะติดตอนเปิดแอปธนาคารไม่ได้ · รายละเอียด "ไม่บังคับ/ไม่มีเกณฑ์" agent เลือกเอง (review ไม่ได้เสนอ) — เปลี่ยนได้ | Task 1 (`qrRefundedSatang`, `qrNetSatang`, `qrDifferenceSatang`) · Task 6 (`bankQrTotalSatang`) · Task 9 (`QrTable`) · Task 11 (`close-bank-qr`) | ✅ (ข) — D53 |
| Q3b-13 | **ความลับของไฟล์สำรอง และใครสำรองได้** — ในไฟล์มี `pin_hash` ของ PIN 4–6 หลัก (เดาแบบออฟไลน์ได้ไม่ว่าจะตั้ง argon2 แรงแค่ไหน) และหมายเลขพร้อมเพย์ของร้าน ไฟล์ไปอยู่ใน Downloads แล้วขึ้น Google Drive และร่างแรกให้ทุกคนที่ล็อกอินสำรองได้ | (ก) ยอมรับ แต่สำรองได้เฉพาะเจ้าของ · (ข) เข้ารหัสไฟล์ด้วยรหัสผ่าน (WebCrypto AES-GCM ในเบราว์เซอร์ ไม่เพิ่ม dependency) · (ค) ยอมรับตามเดิม | **(ก)** | ร้าน 2 คนเป็นเจ้าของทั้งคู่ · การเข้ารหัสเพิ่มรหัสผ่านที่ต้องจำ และถ้าลืม ไฟล์สำรองใช้ไม่ได้ · จำกัดที่ API และหน้าจอ | Task 7 (`exportBackup`/`confirmBackupSaved` → `NOT_OWNER`) · Task 8 (`backup-owner-only`) | ✅ (ก) — D53 |

## จ. คำถามเพิ่มจาก review Task 6 รอบ fix 1b (19 ก.ย.)

| # | คำถาม | ตัวเลือก | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|---|
| Q3b-14 | **จ่ายเงินออก/นำเงินออกไปเก็บ (`PAID_OUT`/`DROP`) เกินเงินสดที่ควรมีในลิ้นชัก ณ ขณะนั้น** ทำได้หรือไม่ (Q3b-9 · D52 ยังไม่ได้ตอบเรื่องนี้) | (ก) บล็อก ไม่ให้บันทึกถ้าจะทำให้ยอดคาดว่าเหลือติดลบ · (ข) บันทึกได้เงียบ ๆ ไม่เตือนอะไร · (ค) บันทึกได้ ไม่บล็อก แต่หน้าจอเตือนด้วยข้อความยืนยันก่อนบันทึกจริง | **(ค)** | ลิ้นชักจริงอาจมีเงินมากกว่าที่ระบบคำนวณได้ (เช่น เจ้าของหยิบเงินส่วนตัวมาเสริมไว้ก่อน) การบล็อกแบบ (ก) จะกันธุรกรรมจริงที่ถูกต้อง · เตือนไว้พอให้รู้ตัวว่าตัวเลขจะติดลบ โดยไม่ขวางทาง | Task 11 (`CashMoveDialog` — เตือนก่อนบันทึกถ้า `PAID_OUT`/`DROP` เกินเงินสดที่ควรมี) | ✅ (ค) — D54 |
| Q3b-15 | **ข้อจำกัดของการนับแบบไม่เห็นยอด (Q3b-3 · D52)** — คนนับที่จำยอดขายวันนั้นได้เองก็อาจคาดเดายอดที่ควรมีได้คร่าว ๆ ก่อนกดนับเสร็จอยู่ดี เพราะหน้า X (ที่เปิดดูได้ตลอดกะ) แสดงยอดขายและบรรทัดเงินสดแต่ละองค์ประกอบ (เงินทอนตั้งต้น/เงินสดขาย/คืนเงินสด/เงินเข้า/เงินออก/นำออกเก็บ) ต้องแก้อะไรเพิ่มหรือไม่ | (ก) ยอมรับข้อจำกัดสำหรับร้าน 2 คน ไม่ต้องแก้เพิ่ม · บันทึกไว้เป็นความเสี่ยงที่รับทราบแล้ว · (ข) ซ่อนบรรทัดองค์ประกอบเงินสดในหน้า X ด้วย ให้ปิดตาจริง 100% · (ค) ล็อกหน้า X ไม่ให้เปิดจนกว่าจะปิดกะ | **(ก)** | ร้าน 2 คนเป็นเจ้าของทั้งคู่ ผลของการ "นับให้ตรง" ไม่ใช่ป้องกันการโกงจากคนนอก แต่กันความประมาท/ลืม ซึ่ง (ก) ทำได้พอแล้ว · (ข)/(ค) ทำให้หน้า X ใช้ตรวจยอดขายกลางวันไม่ได้เลย ขัด spec §4.8 ("X เปิดดูได้ตลอด") | เอกสารเท่านั้น — ไม่มี task ต้องแก้ | ✅ (ก) — D54 |
| Q3b-16 | **Z ใบก่อนหน้าแฮชไม่ตรง และ chain แบบเข้ม (`recomputeZChain`) คำนวณต่อไม่ได้เลย** (net ที่เก็บไว้ไม่ตรงกับ gross−ส่วนลด−ยอดยกเลิก, `zNo` มีช่องว่างหรือซ้ำ, หรือ snapshot อ่านไม่ได้) — review C-1 พบว่าร่างเดิมโยน `BAD_INPUT` ในกรณีนี้ ทำให้ปิดกะไม่ได้อีกเลย ขัดกับ Q3b-11 (D53) ที่ให้ปิดกะได้เสมอหลังรับทราบ | (ก) ครั้งแรกตอบ `Z_CHAIN_BROKEN` เสมอ (ไม่ใช่ `BAD_INPUT`) · หลังรับทราบด้วย PIN คำนวณ chain แบบผ่อนปรนที่ไม่มีวันโยน error: net ของ Z ก่อนหน้าแต่ละใบ = gross−ส่วนลด−ยอดยกเลิก ถ้าตัวเลขสามตัวนี้เป็นจำนวนเต็มไม่ติดลบและสอดคล้องกัน ไม่เช่นนั้นใช้ net ที่เก็บไว้ถ้าอ่านได้ ไม่เช่นนั้นถือเป็น 0 · zNo ใหม่ = จำนวนแถว Z ที่มี + 1 · บันทึกรายชื่อ Z ที่อ่านไม่ได้/ไม่สอดคล้องไว้ใน `chainWarning.unreadableZs` ถาวร · (ข) บล็อกถาวรจนกว่าจะมีคนแก้ไฟล์ด้วยมือ (ซ่อมข้อมูล) · (ค) เหมือน (ก) แต่ไม่บันทึกว่า Z ใดอ่านไม่ได้ | **(ก)** | สอดคล้องกับหลักการเดียวกับ Q3b-11 (D53): ร้านต้องปิดวันได้เสมอ ไม่ว่าข้อมูลเก่าจะเสียแค่ไหน · การไล่กลับไปหา gross/discount/voided ที่เก็บแยกกันยังให้ค่าที่เชื่อได้มากกว่า net ตัวเดียวที่อาจถูกแก้ · รายชื่อ Z ที่อ่านไม่ได้ (แทน (ค)) ช่วยตรวจสอบย้อนหลังว่าความเสียหายมีขนาดแค่ไหน | Task 6 (`recomputeZChainLenient`, `ZChainWarning.unreadableZs`, `close.ts`) · Task 9 (แสดง `unreadableZs` ถ้ามี — งานฝั่งจอ ยังไม่ทำในรอบนี้) | ✅ (ก) — D54 |
| Q3b-17 | **`SHIFT_CHANGED` ต้องครอบตัวเลขอะไรบ้าง** — ร่างเดิมเทียบแค่เงินสดที่ควรมี (`shownExpectedCashSatang`) ทำให้การขาย/ยกเลิกบิลพร้อมเพย์ระหว่างจอปิดกะเปิดอยู่ (ไม่กระทบเงินสด) หลุดผ่านไปแช่แข็งใน Z แบบไม่มีการเตือน (review m-1) | (ก) ฝั่งแอปส่งแฮชของรายงาน X ที่จอแสดง (`shownReportFingerprint` ครอบ sales/cash/varianceAlertSatang/voids) มาด้วย ปิดกะคำนวณแฮชใหม่จากรายงานสดแล้วเทียบ ไม่ตรงก็ปฏิเสธ (`shownExpectedCashSatang` ยังคงไว้เพื่อข้อความ error ที่อ่านง่าย) · (ข) เทียบทุกตัวเลขแยกกันเป็นช่อง ๆ (sales, cash, voids ฯลฯ) ทีละช่อง · (ค) เทียบแค่เงินสดที่ควรมีตามเดิม ยอมรับความเสี่ยงนี้ | **(ก)** | แฮชเดียวครอบทุกตัวเลขได้แน่นอน ไม่ต้องคอยเพิ่มช่องเทียบทุกครั้งที่ Z มีฟิลด์ใหม่ · เบากว่า (ข) ทั้งโค้ดและงานส่งข้อมูล · เก็บเงินสดแยกไว้ด้วยเพื่อข้อความ error ที่อ่านง่าย ("shown X, now Y") | Task 6 (`shiftReportFingerprint`, `CloseShiftInput.shownReportFingerprint`, `close.ts`) · Task 11 (ต้องเรียก `shiftReport()` แล้วส่ง `shownReportFingerprint` กลับมาด้วยตอนปิดกะ) | ✅ (ก) — D54 |

## ฉ. คำถามเพิ่มจาก final review — known risk (h) / finding m-1 (21 ก.ย.)

| # | คำถาม | ตัวเลือก | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|---|
| Q3b-18 | **Z แถวกลางถูกลบทำให้ grand total ลดลงถาวร** แม้ Z ใบสุดท้ายที่ hash ยังถูกจะมีค่าที่ถูกต้องอยู่แล้ว — probe ของ final review: Z1–Z3 (grand ฿140) ลบ Z2 ทิ้ง แล้ว Z4 (+฿50) แช่แข็งด้วย grand **฿140** ทั้งที่ค่าที่ถูกต้องคือ ฿190 เพราะ `recomputeZChainLenient` มองไม่เห็นแถวที่ถูกลบไปแล้ว (known risk (h) · Q3b-16 · D54 ยังทำตามรูล "คำนวณใหม่จาก snapshot ทุกใบ" เดิม) | (ก) เมื่อ Z ใบสุดท้ายยัง **น่าเชื่อถือ** (hash ตรง, snapshot อ่านได้, grand total เป็นจำนวนเต็มไม่ติดลบ — ตัด `zNo === จำนวนแถว` ออกจากเกณฑ์นี้) ต่อ chain จาก `zNo`/`grand` **ที่เก็บไว้ในใบนั้นเอง** (`zNo+1`, grand ที่เก็บไว้) แทนการคำนวณใหม่จากแถวที่เหลือ · ยังตอบ `Z_CHAIN_BROKEN` ก่อนเสมอ ยังต้อง PIN รับทราบ ยังบันทึกช่องว่างไว้ใน `missingZNos` · คำนวณใหม่แบบผ่อนปรนเหมือนเดิมเฉพาะเมื่อ Z ใบสุดท้ายเองไม่น่าเชื่อถือ · (ข) ต่อจาก `max(stored, recomputed)` เสมอ (ทางเลือกที่ final review เสนอไว้) · (ค) ยอมรับความเสี่ยงนี้ตามเดิม ไม่แก้ | **(ก)** | ตัวเลขที่แช่แข็งในแฮชของ Z ใบสุดท้ายที่ valid เชื่อถือได้มากกว่าเสมอ เพราะถูกบันทึกไว้ **ก่อน** ที่ Z กลางจะถูกลบ จึงมี Z กลางรวมอยู่แล้วอย่างถูกต้อง · ง่ายกว่า (ข) (ไม่ต้องเทียบ 2 ค่า) และไม่มีกรณีที่ `stored < recomputed` จะเกิดขึ้นได้เมื่อ Z ใบสุดท้าย valid อยู่แล้ว | Task 6 (`apps/pos/src/api/close.ts` — เกณฑ์ "healthy last Z", `chainWarning.missingZNos`) — ไม่แก้ `packages/domain` เพราะ `buildZReport` ยอมรับผลลัพธ์นี้อยู่แล้ว | ✅ (ก) — D55 |

---

## ตัดสินใจเชิงเทคนิค (ไม่ต้องตอบ — ตามมาจาก spec/D หรือเป็นเรื่องภายในโค้ด · คัดค้านได้)

| # | เรื่อง | ตัดสินใจ | เหตุผล/ที่มา |
|---|---|---|---|
| T3b-1 | เกณฑ์เงินขาด/เกินที่ต้องใส่เหตุผล | setting `cash.variance_alert_satang` (spec §3.1) · ยังไม่มีแถวในเครื่องจึงใช้ค่าเริ่มต้น **2,000 สตางค์ (฿20)** · "เกิน" = \|ส่วนต่าง\| **มากกว่า** เกณฑ์ (ขาด/เกินพอดี ฿20 ไม่ต้องใส่) | spec §3.1, §4.8 ตัดสินไว้แล้ว |
| T3b-2 | รายงาน X | คำนวณสดจากแถวในเครื่อง ดูได้ตลอดกะ ไม่เขียนอะไรลง DB · ไม่แสดงบรรทัดเงินสดที่ควรมีระหว่างกะเปิด (Q3b-3 · D52) | spec §4.8 "X = คำนวณสดจากข้อมูลได้ตลอด" |
| T3b-3 | outbox ที่ยังไม่ส่งตอนปิดกะ | ยังไม่มี sync ในแผนนี้ → ปิดกะ **ไม่รอ ไม่ลบ** แสดงจำนวนค้างบนหน้า X/ปิดกะ · การ push หลังปิดกะเป็นงานแผน 5 (spec §6.2) · ข้อมูลทั้งหมดอยู่ในไฟล์สำรอง | spec §6.2, T15 ของแผน 3 |
| T3b-4 | UPDATE ที่เพิ่มในแผนนี้ | `shift.status`/`closed_by`/`closed_at` ตอนปิดกะ (ตาราง `shift` ไม่มี trigger · คอลัมน์มีใน spec §3.5) · ส่ง outbox แยก key `shift:<id>:closed` แบบเดียวกับ `order:<id>:voided` · `cash_count`, `z_report`, `cash_movement` insert อย่างเดียว · upsert คีย์ local-only ใน `sync_state` ตอนยืนยันไฟล์สำรอง | spec §3.5, T14/T15 ของแผน 3 · review m-2 |
| T3b-5 | ป้องกันปิดกะด้วยยอดเก่า | `closeShift` รับ `shownExpectedCashSatang` (ยอดที่จอแสดงหลังนับ) ถ้าคำนวณใหม่ไม่ตรง → `SHIFT_CHANGED` ไม่เขียนอะไร | หลักเดียวกับ `PRICE_CHANGED` (D50 Q3-27) |
| T3b-6 | grand total | ต่อจาก Z ที่ `zNo` สูงสุด **ของเครื่องนี้** (`shift.device_id`) — ไม่ใช้เวลา เพราะนาฬิกาเครื่องเลื่อนได้ (review I-1) · Z(n).zNo = Z(n−1).zNo + 1 · Z ใบก่อนแฮชไม่ตรง → ทำตาม **Q3b-11 (D53)**: `Z_CHAIN_BROKEN` → เจ้าของกรอก PIN ซ้ำเพื่อรับทราบ → ต่อจากค่าที่คำนวณใหม่จาก snapshot ทุกใบเรียงตาม `zNo` + ธง `chainWarning` ถาวร + `audit_log` (ไม่ปฏิเสธถาวรแบบร่างแรก) | spec §4.8 + invariant 6 ของ §7 · review I-1, I-3 |
| T3b-7 | ความถูกต้องของ Z (Plan 1 notes §4) | `buildZReport` ตรวจ net = gross − ส่วนลด − ยอดยกเลิก · เงินสด + QR = gross − ส่วนลด · ยอดขายเงินสดตรงกันสองฝั่ง · ผลรวมจำนวนเงินที่นับ · รายการบิลยกเลิกครบ · Σ บิลยกเลิกเงินสด = `VOID_REFUND` · Σ บิลยกเลิกพร้อมเพย์ = ยอดโอนคืน · **บังคับเหตุผลเมื่อเกินเกณฑ์** | บันทึกการทำแผน 1 §4 · review m-1 |
| T3b-8 | เบสติดลบที่ต้องแสดงตอนปิดกะ | item `kind='prepared'` + `is_tracked` + `on_hand < 0` · ข้ามของไม่นับสต็อก (น้ำแข็ง ฯลฯ ที่มีแถว on-hand ติดลบไร้ความหมาย) | D28 · notes แผน 3 §5 (ส่งต่อแผน 4) |
| T3b-9 | รายการบิลยกเลิกประจำวัน | อ่านเหตุผล/ผู้อนุมัติ/ทำแล้วหรือยัง/เลขอ้างอิงโอนคืนจาก event `VOIDED` · ชื่อผู้อนุมัติถูกแช่แข็งไว้ใน Z | D50 Q3-22 · notes แผน 3 §5 (อ้างอิงโอนคืนอยู่ใน event ไม่ใช่ `payment.reference`) |
| T3b-10 | เพดานความยาวเหตุผล | `REASON_MAX_LENGTH = 200` ใช้กับเหตุผล void, ส่วนลด, ส่วนต่างเงินสด, เงินเข้า-ออก · นับด้วย `.length` (UTF-16) ให้ตรงกับ `maxLength` ของช่อง input | final review แผน 3 Task 13 M-4 |
| T3b-11 | อ่านไฟล์สำรอง | `exportBackup` รันในคิว serial ของ `PosApi` (ไม่มี transaction ค้าง) · ตรวจ 16 ไบต์แรก `SQLite format 3\0` · **ไม่บันทึกอะไรตอน export** · บันทึกเวลา export + id ของ Z ล่าสุดในไฟล์ใน `sync_state` (`local.last_backup_at`, `local.last_backup_z_id`) + `audit_log` `backup` ตอนเจ้าของกด "บันทึกไฟล์แล้ว" (`confirmBackupSaved` — Q3b-7 · D52 · review I-4) · เทสต์บน Node ใช้ `VACUUM INTO` แทน `exportFile` | spike I3 · spec §11 |
| T3b-12 | รูปแบบ snapshot ของ Z เปลี่ยน | `ZInput` เปลี่ยนเป็น `sales` ซ้อน (รวมพร้อมเพย์โอนคืน/สุทธิ) + `zNo` + `countLines` + `voids` + `openedQuick` + `varianceAlertSatang` + `countedBy` + `bankQrTotalSatang` + `chainWarning` · ปลอดภัยเพราะยังไม่มีแถว `z_report` ที่ใด (ยังไม่ขายจริง — D48 Q3-17) | แผน 1 วาง `ZInput` ไว้ก่อนมีข้อมูลจริง |
