# แผน 3b (pos-shift-close) — คำถามถึงเจ้าของก่อนเริ่มทำ

สถานะ: 🟡 **รอคำตอบ 10 ข้อ (Q3b-1 – Q3b-10)** · แผน [2026-09-17-03b-pos-shift-close.md](2026-09-17-03b-pos-shift-close.md) เขียนตาม **ค่าแนะนำ** ไว้ก่อน และทำเครื่องหมาย `(รอ Q3b-N)` ทุกจุดที่ขึ้นกับคำตอบ · ตอบแล้วบันทึกเป็น D52 ใน [บันทึกการตัดสินใจ](../../design/00-บันทึกการตัดสินใจ.md) · ถ้าคำตอบต่างจากค่าแนะนำ ให้แก้จุดที่ระบุในคอลัมน์ "ใช้ที่" ก่อนเริ่ม task นั้น

กติกา: เรื่องที่ spec และ D1–D51 ตัดสินแล้ว **ไม่ถามซ้ำ** (ดูหัวข้อ "ตัดสินใจเชิงเทคนิค" ท้ายไฟล์) · แต่ละข้อมีตัวเลือก + **ค่าแนะนำ** + เหตุผลบรรทัดเดียว · คำถามใหม่ที่เกิดระหว่างทำให้เพิ่มท้ายไฟล์เป็น Q3b-11 เป็นต้นไป แล้วหยุดรอคำตอบก่อนทำ task ที่เกี่ยวข้อง

---

## ก. ปิดกะและนับเงิน

| # | คำถาม | ตัวเลือก | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|---|
| Q3b-1 | นับเงินปิดกะ นับธนบัตร/เหรียญชนิดไหนบ้าง — spec §5 เขียนตาราง 1000/500/100/50/20/10/5/2/1 แต่ไม่ได้พูดถึงเหรียญสตางค์ (ส่วนลดใส่เศษสตางค์ได้ เช่น 45.50) | (ก) 9 ชนิดตาม spec เท่านั้น · (ข) 9 ชนิด + เหรียญ 50 สตางค์ + 25 สตางค์ · (ค) 9 ชนิด + ช่อง "เศษ/อื่น ๆ" ใส่เป็นบาท | **(ก)** | ราคาเมนูเป็นบาทเต็มทั้งหมด · ส่วนลดมีเศษสตางค์น้อยมาก และส่วนต่างไม่ถึง 1 บาทต่ำกว่าเกณฑ์ ฿20 อยู่แล้ว · เพิ่มชนิดภายหลังแก้ค่าคงที่ตัวเดียว | Task 1 (`CASH_DENOMINATIONS_SATANG`) · Task 10 (`CloseShiftScreen`) | |
| Q3b-2 | ใครปิดกะได้ และยืนยันอย่างไร — spec §5 เขียนว่า "ปิดวันต้อง owner" แต่ไม่ได้บอกวิธียืนยัน | (ก) owner ที่ล็อกอินอยู่กดได้เลย · (ข) คนที่ล็อกอินนับเงิน แล้ว **เจ้าของกรอก PIN ซ้ำ** ทุกครั้ง (เลือกตัวเองได้ เหมือน void — D50 Q3-22) · (ค) ต้องใช้ PIN ของเจ้าของอีกคนเท่านั้น | **(ข)** | ใช้กลไกเดียวกับ void (ตัวนับ PIN ผิด D50 Q3-21) · ถ้ามีพนักงานในอนาคตก็นับเองได้ แต่เจ้าของต้องยืนยัน · Z บันทึกทั้ง `countedBy` และ `closedBy` | Task 6 (`closeShift` → `requireOwnerPin`) · Task 10 (`CloseShiftScreen` เลือกเจ้าของ + PinPad) | |
| Q3b-3 | ตอนนับเงินปิดกะ ให้เห็น "เงินสดที่ควรมี" ก่อนนับหรือไม่ (นับแบบปิดตา — blind count) | (ก) **นับก่อน** กด "นับเสร็จ" แล้วจึงเห็นยอดที่ควรมีและส่วนต่าง (แก้จำนวนที่นับได้ก่อนยืนยัน) · (ข) เห็นยอดที่ควรมีตั้งแต่เริ่มนับ | **(ก)** | กันการนับให้ "ตรง" ตามตัวเลขที่เห็น ทำให้ส่วนต่างสะท้อนความจริง · รายงาน X ยังเปิดดูได้ตลอดตาม spec §4.8 (ร้าน 2 คน ไม่ได้ซ่อนจากเจ้าของ) | Task 10 (`CloseShiftScreen` ขั้น `counted`) | |
| Q3b-4 | ยอดขายสุทธิ (net) และยอดสะสม grand total ใน Z **นับบิลที่ยกเลิกหรือไม่** — spec §4.3 บอกว่าบิลยกเลิก "อยู่ใน gross และแยกเป็นยอดยกเลิก" แต่ §4.8 บอกแค่ "grand = Z ก่อนหน้า + ยอดกะนี้" | (ก) net = gross − ส่วนลด − ยอดบิลยกเลิก (= ยอดบิลที่ยังชำระอยู่) · grand total สะสม net · (ข) net = gross − ส่วนลด (รวมบิลยกเลิก) · grand total สะสมตัวนี้ | **(ก)** | net ต้องเท่ากับเงินที่ร้านได้จริงจากการขาย · grand total ใช้เทียบเกณฑ์ VAT 1.8 ล้าน (D8) จึงไม่ควรนับบิลที่คืนเงินไปแล้ว · gross และยอดยกเลิกยังแสดงแยกครบ | Task 1 (`summarizeShiftSales`, `assertSalesSummary`) · Task 6 (Z) | |
| Q3b-5 | รายงาน Z แสดงหรือส่งออกอย่างไร (ยังไม่มีเครื่องพิมพ์ — D4) | (ก) แสดงบนจอทันทีหลังปิดกะ + ดู Z ย้อนหลังทุกใบในเครื่องได้ · ไม่ส่งออกแยก · (ข) (ก) + ปุ่มบันทึก Z เป็นไฟล์ JSON/CSV · (ค) (ก) + แชร์เป็นรูปภาพ | **(ก)** | Z อยู่ในไฟล์สำรองฐานข้อมูลอยู่แล้ว (Q3b-6) · export CSV เป็นงานรายงานของแผน 7 · ลดงานที่ต้องทดสอบบนแท็บเล็ต | Task 6 (`listZReports`, `getZReport`) · Task 9 (`ZReportScreen`, `ZListScreen`) | |

## ข. สำรองไฟล์ฐานข้อมูล

| # | คำถาม | ตัวเลือก | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|---|
| Q3b-6 | ปุ่มสำรองไฟล์: ส่งไฟล์ไปที่ไหน รูปแบบอะไร | (ก) ไฟล์ SQLite ทั้งไฟล์ (`pool.exportFile` — ผล spike I3) **ดาวน์โหลดลงโฟลเดอร์ Downloads** ชื่อ `dayo-pos-A-YYYYMMDD-HHmmss.sqlite3` แล้วเจ้าของย้ายไป Google Drive/คอมเอง · (ข) (ก) + ปุ่ม "แชร์" (Web Share) ส่งเข้า Drive/LINE ตรง ๆ · (ค) รอแผน 5 (สำรองขึ้นเซิร์ฟเวอร์) ไม่ทำปุ่มในเครื่อง | **(ก)** | ใช้ได้ทันทีแบบออฟไลน์ ไม่ต้องมีบัญชีใด · Chrome อนุญาตแชร์ไฟล์เฉพาะบางนามสกุล และ `.sqlite3` น่าจะไม่อยู่ในรายการ (ต้องลองบนแท็บเล็ตจริงถ้าเลือก (ข)) · (ค) ขัด D48 Q3-17 ที่ต้องมีสำรองก่อนขายจริง | Task 7 (`exportBackup`, ชื่อไฟล์) · Task 8 (`BackupScreen`, `downloadBytes`) | |
| Q3b-7 | สำรองบ่อยแค่ไหน และบังคับหรือไม่ — spec §11 เขียนว่า "export ไฟล์ SQLite ทุกคืนชั่วคราว" | (ก) หลังปิดกะ หน้า Z ชวนสำรอง + **แถบเตือนสีแดง** ที่หน้า Z และหน้าเปิดกะ จนกว่าจะสำรองหลัง Z ใบล่าสุด · ไม่บล็อกการเปิดกะ · (ข) บังคับสำรองก่อนเปิดกะใหม่ได้ · (ค) มีแค่ปุ่ม ไม่เตือน | **(ก)** | เตือนชัดทุกวันโดยไม่ทำให้เปิดร้านไม่ได้ ถ้าเครื่องมีปัญหาเรื่องดาวน์โหลด · ถ้า S9 (persistent storage) ไม่ผ่านบนแท็บเล็ต (F4) ค่อยเปลี่ยนเป็น (ข) | Task 7 (`BootstrapState.backupDue`) · Task 9 (`ZReportScreen`) · Task 10 (`OpenShiftScreen`) | |

## ค. เรื่องอื่นในกะ

| # | คำถาม | ตัวเลือก | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|---|
| Q3b-8 | ลืมปิดกะข้ามวัน ทำอย่างไร — spec §4.7 ให้ขายหลังเที่ยงคืนยังเป็นวันทำการเดิม และ D22 ให้ 1 กะ = 1 วันทำการ แต่ไม่ได้บอกว่าเมื่อไรถือว่า "ลืมปิด" | (ก) **เตือนแถบแดงบนหน้าขาย** ตั้งแต่ 05:00 ของวันถัดไป (เวลาไทย) ขายต่อได้ · (ข) บังคับปิดกะก่อนขายต่อเมื่อถึง 05:00 · (ค) ไม่ทำอะไร | **(ก) เวลา 05:00** | ไม่ทำให้ขายไม่ได้กลางคัน แต่เห็นชัดตั้งแต่เช้า · ร้านไม่เปิดถึงตี 5 จึงไม่ชนการขายดึกที่ตั้งใจ | Task 7 (`isShiftStale`, `STALE_SHIFT_CUTOFF_HOURS`) · Task 10 (`SellScreen` แถบ `shift-stale`) | |
| Q3b-9 | บันทึก **เงินเข้า / จ่ายออก / นำเงินออกไปเก็บ** (`PAID_IN`/`PAID_OUT`/`DROP`) ทำในแผน 3b หรือไม่ — สูตร D36 มีสามตัวนี้ แต่แผน 3 ยังไม่มีหน้าจอบันทึก ถ้าไม่ทำ เช่นหยิบเงินลิ้นชักไปซื้อน้ำแข็ง ปิดกะจะขาดทุกครั้ง | (ก) **ทำใน 3b**: ปุ่ม "เงินเข้า-ออก" บนหน้าขาย · ทุกคนที่ล็อกอินบันทึกได้ · บังคับเหตุผล · (ข) ทำ แต่ `PAID_OUT`/`DROP` ต้อง PIN เจ้าของ · (ค) ไม่ทำ (รอแผน 4/7) | **(ก)** | ปิดกะจะตรงก็ต่อเมื่อบันทึกเงินออกได้ · ร้าน 2 คนเป็นเจ้าของทั้งคู่ · ทุกแถวมีคนทำ + เหตุผล + เข้า X/Z | Task 4 (`recordCashMovement`) · Task 10 (`CashMoveDialog`) | |
| Q3b-10 | **"เปิดกะด่วน"** (spec §4.8: owner เปิดด้วยเงินทอน 0 "พร้อม event"; แผน 3 M18) — ตาราง `order_event` ผูกกับบิลเท่านั้น จะเก็บ "event" ของการเปิดกะด่วนไว้ที่ไหน และยืนยันตัวอย่างไร | (ก) แถว `audit_log` (`entity='shift'`, `action='quick_open'`) ในธุรกรรมเดียวกับการเปิดกะ + ธง `openedQuick` ใน snapshot ของ Z (ซิงก์ไปกับ `z_report` และมีแฮชคุม) · เจ้าของที่ล็อกอินกดได้เลย · **ไม่แก้ schema** · (ข) เพิ่มคอลัมน์ `shift.open_kind` (migration ทั้ง sqlite และ pg) · (ค) (ก) แต่ต้องกรอก PIN เจ้าของซ้ำ | **(ก)** | ไม่ต้องแก้ schema 2 dialect กลางทาง · การเปิดกะด่วนไม่มีเงินเข้าออก ความเสี่ยงต่ำ · `audit_log` เป็น local-only ในแผน 3 (notes §5) แต่ธงใน Z ซิงก์ขึ้นเซิร์ฟเวอร์ได้ | Task 3 (`quickOpenShift`, `wasQuickOpened`) · Task 5/6 (`openedQuick`) · Task 10 (`OpenShiftScreen` ปุ่ม `shift-quick-open`) | |

---

## ตัดสินใจเชิงเทคนิค (ไม่ต้องตอบ — ตามมาจาก spec/D หรือเป็นเรื่องภายในโค้ด · คัดค้านได้)

| # | เรื่อง | ตัดสินใจ | เหตุผล/ที่มา |
|---|---|---|---|
| T3b-1 | เกณฑ์เงินขาด/เกินที่ต้องใส่เหตุผล | setting `cash.variance_alert_satang` (spec §3.1) · ยังไม่มีแถวในเครื่องจึงใช้ค่าเริ่มต้น **2,000 สตางค์ (฿20)** · "เกิน" = \|ส่วนต่าง\| **มากกว่า** เกณฑ์ (ขาด/เกินพอดี ฿20 ไม่ต้องใส่) | spec §3.1, §4.8 ตัดสินไว้แล้ว |
| T3b-2 | รายงาน X | คำนวณสดจากแถวในเครื่อง ดูได้ตลอดกะ ไม่เขียนอะไรลง DB | spec §4.8 "X = คำนวณสดจากข้อมูลได้ตลอด" |
| T3b-3 | outbox ที่ยังไม่ส่งตอนปิดกะ | ยังไม่มี sync ในแผนนี้ → ปิดกะ **ไม่รอ ไม่ลบ** แสดงจำนวนค้างบนหน้า X/ปิดกะ · การ push หลังปิดกะเป็นงานแผน 5 (spec §6.2) · ข้อมูลทั้งหมดอยู่ในไฟล์สำรอง | spec §6.2, T15 ของแผน 3 |
| T3b-4 | UPDATE ที่เพิ่มในแผนนี้ | `shift.status`/`closed_by`/`closed_at` ตอนปิดกะเท่านั้น (ตาราง `shift` ไม่มี trigger · คอลัมน์มีใน spec §3.5) · ส่ง outbox แยก key `shift:<id>:closed` แบบเดียวกับ `order:<id>:voided` · `cash_count`, `z_report`, `cash_movement` insert อย่างเดียว | spec §3.5, T14/T15 ของแผน 3 |
| T3b-5 | ป้องกันปิดกะด้วยยอดเก่า | `closeShift` รับ `shownExpectedCashSatang` (ยอดที่จอแสดงหลังนับ) ถ้าคำนวณใหม่ไม่ตรง → `SHIFT_CHANGED` ไม่เขียนอะไร | หลักเดียวกับ `PRICE_CHANGED` (D50 Q3-27) |
| T3b-6 | grand total | ต่อจาก Z ใบล่าสุด **ของเครื่องนี้** (`shift.device_id`) · Z ใบก่อนแฮชไม่ตรง → ปฏิเสธปิดกะ (`BAD_INPUT`) ไม่ต่อยอดจากข้อมูลที่ถูกแก้ | spec §4.8 + invariant 6 ของ §7 |
| T3b-7 | ความถูกต้องของ Z (Plan 1 notes §4) | `buildZReport` ตรวจ net = gross − ส่วนลด − ยอดยกเลิก · เงินสด + QR = gross − ส่วนลด · ยอดขายเงินสดตรงกันสองฝั่ง · ผลรวมจำนวนเงินที่นับ · รายการบิลยกเลิกครบ · **บังคับเหตุผลเมื่อเกินเกณฑ์** | บันทึกการทำแผน 1 §4 |
| T3b-8 | เบสติดลบที่ต้องแสดงตอนปิดกะ | item `kind='prepared'` + `is_tracked` + `on_hand < 0` · ข้ามของไม่นับสต็อก (น้ำแข็ง ฯลฯ ที่มีแถว on-hand ติดลบไร้ความหมาย) | D28 · notes แผน 3 §5 (ส่งต่อแผน 4) |
| T3b-9 | รายการบิลยกเลิกประจำวัน | อ่านเหตุผล/ผู้อนุมัติ/ทำแล้วหรือยัง/เลขอ้างอิงโอนคืนจาก event `VOIDED` · ชื่อผู้อนุมัติถูกแช่แข็งไว้ใน Z | D50 Q3-22 · notes แผน 3 §5 (อ้างอิงโอนคืนอยู่ใน event ไม่ใช่ `payment.reference`) |
| T3b-10 | เพดานความยาวเหตุผล | `REASON_MAX_LENGTH = 200` ใช้กับเหตุผล void, ส่วนลด, ส่วนต่างเงินสด, เงินเข้า-ออก · นับด้วย `.length` (UTF-16) ให้ตรงกับ `maxLength` ของช่อง input | final review แผน 3 Task 13 M-4 |
| T3b-11 | อ่านไฟล์สำรอง | `exportBackup` รันในคิว serial ของ `PosApi` (ไม่มี transaction ค้าง) · ตรวจ 16 ไบต์แรก `SQLite format 3\0` · บันทึกเวลาใน `sync_state` (`local.last_backup_at`) + `audit_log` `backup` **หลัง** คัดลอก · เทสต์บน Node ใช้ `VACUUM INTO` แทน `exportFile` | spike I3 · spec §11 |
| T3b-12 | รูปแบบ snapshot ของ Z เปลี่ยน | `ZInput` เปลี่ยนเป็น `sales` ซ้อน + `countLines` + `voids` + `openedQuick` + `varianceAlertSatang` + `countedBy` · ปลอดภัยเพราะยังไม่มีแถว `z_report` ที่ใด (ยังไม่ขายจริง — D48 Q3-17) | แผน 1 วาง `ZInput` ไว้ก่อนมีข้อมูลจริง |
