# แผน 3 (pos-sell) — คำถามถึงเจ้าของก่อนเริ่มทำ

สถานะ: 🟢 **ตอบครบ 19 ข้อ (Q3-1 – Q3-19)** เมื่อ 18 ก.ย. 2026 — บันทึกเป็น D44, D46, D47 ข้อ 2 และ D48 ใน [บันทึกการตัดสินใจ](../../design/00-บันทึกการตัดสินใจ.md) · ใส่คำตอบลงในแผน [2026-09-17-03-pos-sell.md](2026-09-17-03-pos-sell.md) แล้ว (ไม่มีจุด `รอคำตอบ` เหลือ) · ไม่มีคำถามใหม่ที่เปิดอยู่

กติกา: ทุกเรื่องที่ spec และ D1–D49 ยังไม่ได้ตัดสิน ถามเจ้าของ ไม่ตัดสินเอง · แต่ละข้อมี **ค่าแนะนำ** + เหตุผลบรรทัดเดียว · คำถามใหม่ที่เกิดระหว่างทำให้เพิ่มท้ายไฟล์เป็น Q3-20 เป็นต้นไป แล้วหยุดรอคำตอบก่อนทำ task ที่เกี่ยวข้อง

---

## ก. อุปกรณ์และการเปิดแอป

| # | คำถาม | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|
| Q3-1 | แท็บเล็ตที่จะใช้ขายจริงคือรุ่นอะไร ระบบอะไร (Android/iPadOS + เวอร์ชัน) และจะเปิดแอปด้วยเบราว์เซอร์อะไร | แท็บเล็ต Android + **Chrome** เวอร์ชันล่าสุด (ถ้ามี iPad อยู่แล้ว: iPadOS 17 ขึ้นไป + Safari) | Chrome รองรับ OPFS/SQLite WASM นิ่งที่สุด · spike ต้องทดสอบบนเครื่องจริงเครื่องนี้ ไม่ใช่เครื่องสมมติ | Task 1 (เป้าหมาย spike), Task 15 | ✅ **D46** — Android + Chrome · รุ่นยังไม่กำหนด → spike ทดสอบบน Chrome Android ที่มี และทดสอบซ้ำเมื่อได้เครื่องจริง |
| Q3-2 | ก่อนมีแผน 5 (deploy) จะเปิดแอปบนแท็บเล็ตด้วยวิธีไหน — (ก) Cloudflare Quick Tunnel จากเครื่อง dev ชั่วคราว (ข) สมัคร Cloudflare Pages แล้ว deploy ตั้งแต่แผน 3 (ค) USB + `adb reverse` (Android เท่านั้น) | spike ใช้ **(ก)** · ถ้าจะใช้ขายจริงหลังแผน 3 ให้ทำ **(ข)** เป็นงานเล็กแยกหลังแผน 3 | OPFS ใช้ได้เฉพาะ https หรือ localhost · (ก) ไม่ต้องมีบัญชี · ขายจริงต้องมี URL ถาวร | Task 1 Step 9, Task 15 | ✅ **D48** — ตามค่าแนะนำ: spike ผ่าน Quick Tunnel · deploy Pages เป็นงานเล็กแยกหลังแผน 3 |

## ข. การรับเงิน

| # | คำถาม | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|
| Q3-3 | หมายเลขพร้อมเพย์ของร้านเป็นแบบไหน และเป็นบัญชีของใคร | กรอกในหน้า "ตั้งค่าเครื่องครั้งแรก" (ไม่ใส่ใน repo) · ใช้บัญชีที่ต้องการให้เงินเข้า | เป็นข้อมูลส่วนตัว ไม่ควรอยู่ใน git · โค้ดรองรับทั้ง 3 แบบ | Task 6, Task 11, Task 15 Step 4 | ✅ **D48** — กรอกในหน้าตั้งค่าครั้งแรก ไม่อยู่ใน repo |
| Q3-4 | QR พร้อมเพย์: ใส่ยอดของบิลลงใน QR ทุกครั้ง หรือใช้ QR คงที่ไม่มียอด | **ใส่ยอดทุกบิล** (สร้างในเครื่อง ไม่ต้องใช้เน็ต) | ตรงกับ D7/§5 · ลูกค้าไม่พิมพ์ยอดผิด | Task 2, 11, 12, 14 | ✅ **D48** — QR ใส่ยอดทุกบิล |
| Q3-5 | แผน 3 ไม่พิมพ์ใบเสร็จ — ยืนยันว่าหลังชำระ **แสดงเลขคิว + เลขที่บิลบนจออย่างเดียว** | ยืนยันตาม D4 · แสดงบนจอเท่านั้น | ยังไม่มีเครื่องพิมพ์ (D2) | Task 12 (`DoneScreen`) | ✅ **D48** — ไม่พิมพ์ แสดงเลขคิว + เลขบิลบนจอ |
| Q3-6 | ส่วนลดท้ายบิล: (1) จำนวนเงินหรือ % (2) บังคับเหตุผลไหม (3) ใครอนุมัติ (4) ลบรายการจนยอดต่ำกว่าส่วนลดแล้วทำอย่างไร | (1) **จำนวนเงินอย่างเดียว** (2) **บังคับเหตุผล** (3) **คนที่ล็อกอินอยู่** (4) **ลบส่วนลดทิ้ง** ให้ใส่ใหม่ | ตรง spec §1.2/§3.4 และ D21 | Task 8, 9, 10 | ✅ **D48** — ตามค่าแนะนำทั้ง 4 ข้อ |

## ค. หน้าขาย

| # | คำถาม | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|
| Q3-7 | ตะกร้าก่อนชำระ: เก็บในหน่วยความจำแล้วบันทึกครั้งเดียวตอนกดชำระ หรือบันทึกทุกครั้งที่เพิ่ม/ลบรายการ | **เก็บในหน่วยความจำ บันทึกตอนชำระ** | เร็ว ไม่มีบิลค้างครึ่งทาง | Task 8, Task 10 | ✅ **D48** — ตะกร้าในหน่วยความจำ บันทึกตอนชำระ |
| Q3-8 | แตะเมนูเดิม ขนาดเดิม ความหวานเดิมซ้ำ ให้รวมบรรทัดหรือแยกบรรทัด | **รวมบรรทัด** | ตะกร้าสั้น อ่านง่าย | Task 8 | ✅ **D48** — รวมบรรทัด |
| Q3-9 | แท็บ "ขายดี" (D27): นับจากช่วงไหน แสดงกี่เมนู | **7 วันทำการล่าสุด · 8 เมนู** · ซ่อนแท็บถ้ายังไม่มียอดขาย | 8 ปุ่มพอดีหนึ่งหน้าจอ | Task 8, 9, 10 | ✅ **D48** — 7 วัน 8 เมนู |
| Q3-10 | แตะเมนู: popup ทุกครั้ง หรือแตะครั้งเดียวใส่ค่าเริ่มต้นทันที | **popup ทุกครั้งตาม spec** (ค่าเริ่มต้นถูกเลือกไว้แล้ว) | ลดการสั่งผิดขนาด | Task 9 | ✅ **D48** — popup ทุกครั้ง |
| Q3-11 | ปุ่มเมนูแสดงชื่อแบบไหน | **ไทยตัวใหญ่ + อังกฤษตัวเล็กบรรทัดล่าง** | คนทำเครื่องดื่มจำรหัสจาก Excel ได้ ลูกค้าอ่านไทย | Task 9 | ✅ **D48** — ไทยใหญ่ + อังกฤษเล็ก |
| Q3-12 | เปิดกะ: ช่องเงินทอนตั้งต้นมีค่าเริ่มต้นไหม | **ไม่มีค่าเริ่มต้น ต้องกรอกเอง (0 ได้)** | บังคับให้นับจริงทุกเช้า | Task 7 | ✅ **D48** — ไม่มีค่าเริ่มต้น |

## ง. ยกเลิกบิล

| # | คำถาม | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|
| Q3-13 | ยกเลิกได้เฉพาะบิลใน **กะที่เปิดอยู่** หรือย้อนไปบิลของกะที่ปิดแล้วได้ | **เฉพาะกะที่เปิดอยู่** | เงินคืนต้องออกจากลิ้นชักของกะนี้ · Z ที่ปิดแล้วห้ามเปลี่ยน · คืนเงินย้อนหลังทำด้วยมือไปก่อน | Task 11, Task 13 | ✅ **D47 ข้อ 2** — เฉพาะบิลในกะที่เปิดอยู่ · บิลเก่าคืนเงินด้วยมือ (แทนค่าแนะนำ (ก) ของคำถาม Q2 ในแผน 2) |
| Q3-14 | เหตุผลการยกเลิก: มีปุ่มสำเร็จรูป + พิมพ์เองได้ไหม | ปุ่ม: **"ลูกค้าเปลี่ยนใจ" · "กดผิดเมนู" · "ทำผิดสูตร"** + ช่องพิมพ์เอง | กดเร็ว · รายงานจัดกลุ่มได้ | Task 3, Task 14 | ✅ **D48** — 3 ปุ่ม + พิมพ์เอง |
| Q3-15 | ยกเลิกบิล QR: **บังคับ** ใส่เลขอ้างอิงการโอนคืนไหม | **บังคับ** | spec §4.3 · กันลืมโอนคืน | Task 13, Task 14 | ✅ **D48** — บังคับเลขอ้างอิง |

## จ. ข้อมูลเริ่มต้นและขอบเขต

| # | คำถาม | ค่าแนะนำ | เหตุผล | ใช้ที่ | คำตอบ |
|---|---|---|---|---|---|
| Q3-16 | ยังไม่มีเซิร์ฟเวอร์ — สร้างผู้ใช้และเครื่องอย่างไร | หน้า **"ตั้งค่าเครื่องครั้งแรก"**: ชื่อเครื่อง · prefix `A` · เจ้าของ **TungAo, DCm** + PIN · พร้อมเพย์ | ขายได้โดยไม่รอเซิร์ฟเวอร์ | Task 6 | ✅ **D48** — หน้าตั้งค่าครั้งแรก (TungAo, DCm) |
| Q3-17 | แยกปิดกะ/Z + ปุ่มสำรองไฟล์ฐานข้อมูลเป็น **แผน 3b** ได้ไหม | **แยกเป็นแผน 3b** และ **ยังไม่ใช้ขายจริงจนกว่า 3b เสร็จ** | ร้านต้องปิดกะได้และมีสำรองข้อมูลก่อนใช้จริง | ทั้งแผน, README แผน | ✅ **D48** — แยกแผน 3b · ห้ามขายจริงก่อน 3b เสร็จ · ใส่ใน README แผนแล้ว |
| Q3-18 | ข้อความภาษาไทยบนจอ (ตาราง `TH`) — ใช้ตามร่างได้ไหม | **ใช้ตามร่าง** | แก้ทีหลังได้ที่ไฟล์เดียว | Task 3 | ✅ **D48** — ข้อความไทยตามร่าง |
| Q3-19 | สีหลัก/โลโก้ของแอป | ~~สีส้มชาไทย `#c2410c` ไม่มีโลโก้~~ | — | Task 3 | ✅ **D44** — ชุดโลโก้ DA-YO: Deep Forest `#1E3B22` · Cream `#F6F3E8` · Matcha `#7EA05F` (ห้ามเป็นตัวอักษร) · ส้มป้าย `#ED7A13` · สไตล์ป้ายร้าน · ไอคอน `appicon_cream` · โลโก้เล็กตัดสโลแกน (ค่าแนะนำเดิมถูกยกเลิก) |

---

## ตัดสินใจเชิงเทคนิค (ไม่ต้องตอบ — ตามมาจาก spec หรือเป็นเรื่องภายในโค้ด · คัดค้านได้)

| # | เรื่อง | ตัดสินใจ | เหตุผล/ที่มา |
|---|---|---|---|
| T1 | VFS ของ SQLite WASM | `opfs-sahpool` (`installOpfsSAHPoolVfs`) เป็นหลัก | ไม่ต้องตั้ง header COOP/COEP · เร็ว · ใช้ได้บน Chrome (D46) · ข้อเสีย: เปิดได้แท็บเดียว (แอปขายใช้แท็บเดียวอยู่แล้ว) · ทางสำรองอยู่ใน Task 1 |
| T2 | Drizzle ในเครื่อง | `drizzle-orm/sqlite-proxy` ทั้งใน Worker (ครอบ oo1 ของ sqlite-wasm) และในเทสต์ (ครอบ `node:sqlite`) → โค้ด repository ชุดเดียวกันทั้งสองที่ | ตรวจแล้ว: `transaction()` ของ sqlite-proxy ส่ง `begin/commit/rollback` ผ่าน callback · rollback ทำงานจริง (drizzle 0.45.2) |
| T3 | ทุกคำสั่งรันเป็นคิวทีละคำสั่ง | `createSerialQueue()` ครอบทุกเมธอดของ API | connection เดียว + transaction แบบ BEGIN/COMMIT ห้ามซ้อนกัน |
| T4 | ตรรกะทั้งหมดรันใน Worker | UI เรียก `PosApi` ผ่าน Comlink · 1 การกดของผู้ใช้ = 1 round-trip = 1 transaction | §6.1 "ทุกการเขียนเป็น transaction เดียว" |
| T5 | migration ฝั่งเบราว์เซอร์ | **แก้ 18 ก.ย.:** `migrateSqliteRemote` เรียก `dialect.migrate` ของ drizzle (ตัวเดียวกับ `migrateSqlite` ของแผน 2) ด้วย SQL ที่ฝังเป็นโมดูล `sqlite-migrations.gen.ts` (สร้างจาก `drizzle/sqlite` · commit ไว้ + เทสต์ตรวจความสด) · ใช้ตาราง `__drizzle_migrations` เดียวกัน (ไม่มี `__dayo_migrations`) · ปิด FK นอก transaction → migrate → `foreign_key_check` → เปิด FK เสมอ · SQL แปลง CRLF เป็น LF ก่อนคิด hash (drizzle ตัดสินจาก `created_at` เท่านั้น) | แผน 2 M12: migrator ของ drizzle เป็นเจ้าของ migration ของเครื่องเพียงตัวเดียว · มีเทสต์ว่า DB ที่ migrate ด้วยตัวหนึ่ง อีกตัวไม่ต้องทำอะไรเพิ่ม |
| T6 | id ของข้อมูลอ้างอิงจาก seed | **แก้ 18 ก.ย.:** `canonicalSeedOpts()` = `deterministicSeedId('dayo-seed-v1', kind, naturalKey)` ของแผน 2 (UUIDv8 จาก SHA-256 ผูกกับ natural key ไม่ใช่ลำดับ) + `SEED_EFFECTIVE_FROM = 2026-01-01T00:00:00.000Z` · อยู่ใน `@dayo/db-schema` (root และ `/browser`) · เทสต์ pin id 2 ค่า | เครื่องและเซิร์ฟเวอร์ (แผน 5) ต้องได้ id ชุดเดียวกัน · **แผน 5 ต้องเรียก `canonicalSeedOpts()`** · seed ใช้ `applySeedSqlite` ของแผน 2 ไม่มีชุดที่สอง (M11) |
| T7 | ตัวตนของเครื่องนี้ | `sync_state` key `local.device_id` | ตาราง `device` จะมีหลายเครื่องหลัง sync |
| T8 | แฮช PIN | argon2id (`@noble/hashes`) t=2, m=19456 KiB, p=1, salt 16 ไบต์ · เทสต์ใช้ t=1, m=64 | spec §3.1 ระบุ argon2 · ค่า OWASP ขั้นต่ำ · spike วัดเวลาบนแท็บเล็ต |
| T9 | เลขที่บิล | อ่าน `receipt_no` ล่าสุดของเครื่องใน transaction เดียวกับการ insert บิล (`nextReceiptNo`) ไม่มีตาราง counter | Worker เป็นผู้เขียนคนเดียว + คิวทีละคำสั่ง → ไม่ชน · rollback ไม่กินเลข |
| T10 | เลขคิว | `max(queue_no)` ของ (เครื่อง, business_date) + 1 ใน transaction เดียวกับ insert | §4.7 รีเซ็ตทุกวันทำการ · DB บังคับ `unique(device_id, business_date, queue_no)` (D47 ข้อ 5) |
| T11 | business_date | วันที่ตามเวลาไทย (Asia/Bangkok) ตอนเปิดกะ · บิลใช้ `shift.business_date` | §4.7 "ขายหลังเที่ยงคืนยังเป็นวันเดิม" |
| T12 | movement `SALE` | รวมต่อ item ต่อบิล (ไม่แยกต่อบรรทัด) | ลดจำนวนแถว · ตรวจ invariant 4 ได้เพราะแต่ละบรรทัดเก็บ `recipe_id` |
| T13 | `order.cost_satang` | Σ (`unit_cost_satang` × qty) ของทุกบรรทัด | ให้ตรงกับ snapshot ต่อบรรทัด |
| T14 | UPDATE ที่อนุญาต | เฉพาะ `order.status` (paid→voided) + `order.voided_at` + upsert แคช `item_cost_state` · อย่างอื่น insert อย่างเดียว · `stock_movement`/`order_event`/`cash_movement`/`z_report` มี trigger ของแผน 2 ที่ abort UPDATE/DELETE | spec §3.4, §6.5 · ความจริงของประวัติอยู่ที่ `order_event` |
| T15 | outbox | เฉพาะตาราง T · `idempotency_key` = `<table>:<id>[:suffix]` · `status = 'pending'` ตอนเขียน (แผน 2 M8) · ตัวนับ "ยังไม่ส่ง" นับ `status = 'pending'` · ผู้ใช้/เครื่อง/ตั้งค่าที่สร้างตอนตั้งค่าเครื่องไม่เข้า outbox — **แผน 5 ตัดสินวิธีลงทะเบียน** | §6.1 · แผน 3 เขียนอย่างเดียว ไม่ส่ง |
| T16 | event ตอนขาย | `CREATED` → `LINE_ADDED` × n → `DISCOUNT_APPLIED` (ถ้ามี) → `PAID` → `STOCK_DEDUCTED` · ยกเลิก: `VOIDED` → `STOCK_RETURNED` (ถ้ายังไม่ทำ) · โซ่ต่อเครื่อง `chain_id = device_id` | §3.4, §4.9, D38, D39, D48 Q3-7 |
| T17 | commitSale ซ้ำ | `orderId` สร้างตอนเริ่มตะกร้า · ส่งซ้ำคืนผลเดิม ไม่สร้างบิลใหม่ | กันบิลซ้ำ |
| T18 | PromptPay payload | เขียนเองใน `packages/domain/src/promptpay.ts` (EMVCo + CRC-16/CCITT-FALSE) · ค่าทดสอบตรวจเทียบกับไลบรารี `promptpay-qr` แล้ว 4 ชุด | logic เงินอยู่ใน domain · ไม่เพิ่ม dependency runtime |
| T19 | วาด QR | `qrcode` 1.5.4 `toDataURL` | เล็ก ใช้ในเบราว์เซอร์ได้ ไม่ต้องใช้เน็ต |
| T20 | PWA ออฟไลน์ | `vite-plugin-pwa` 1.3.0 precache ทุกไฟล์รวม `.wasm` และ `.png` (สูงสุด 8 MB) · `registerType: 'autoUpdate'` | เปิดแอปได้ตอนไม่มีเน็ต |
| T21 | React Query | `networkMode: 'always'` | ค่าเริ่มต้นจะหยุด query ตอนออฟไลน์ ทั้งที่ข้อมูลอยู่ในเครื่อง |
| T22 | Router | TanStack Router แบบ code-based | เครื่องมือน้อยลง · 10 หน้า |
| T23 | error ข้าม Worker | `PosError` ข้อความขึ้นต้น `CODE:` · UI แปลงเป็นข้อความไทยที่ `ui/errors.ts` | Comlink ส่งแค่ message/name/stack |
| T24 | เงินคืนตอนยกเลิกเงินสด | `cash_movement` kind `VOID_REFUND` ผูกกะที่เปิดอยู่ + `order_id` ของบิล · จำนวน = Σ payment CASH · เขียนเฉพาะเมื่อ > 0 | D36 · CHECK ของ D47 ข้อ 7 |
| T25 | เครื่องมือทดสอบ | unit + integration ของแอปและเทสต์ `browser-*` ของ db-schema: Vitest 5 + `node:sqlite` ผ่าน sqlite-proxy · เทสต์เดิมของแผน 2 ยังใช้ sql.js · component: Testing Library + jsdom · e2e: Playwright Chromium (CI) · เครื่องจริง: checklist มือ | better-sqlite3 build ไม่ผ่านบน Windows · `node:sqlite` ต้อง Node ≥ 22.13 (ตรวจบน 22.23.2) · เปิดไฟล์ DB จาก sql.js ได้ จึงใช้เทียบ migrator สองตัว |
| T26 | แอป spike | เก็บไว้ที่ `apps/pos-spike` ใช้ทดสอบซ้ำเมื่อเปลี่ยนแท็บเล็ต/เบราว์เซอร์ · ไม่อยู่ใน `pnpm test` | spike คือเกณฑ์รับเครื่อง (D46 ต้องรันซ้ำเมื่อได้เครื่องจริง) |
| T27 | เวอร์ชันที่ pin (ตรวจด้วย `pnpm view` วันที่ 2026-09-17) | `@sqlite.org/sqlite-wasm` 3.53.4-build1 · `drizzle-orm` 0.45.2 · `react`/`react-dom` 19.3.0 · `@tanstack/react-router` 1.170.38 · `@tanstack/react-query` 5.103.1 · `vite` 8.3.0 · `@vitejs/plugin-react` 6.1.1 · `vite-plugin-pwa` 1.3.0 · `@playwright/test` 1.63.0 · `comlink` 4.4.2 · `qrcode` 1.5.4 · `@types/qrcode` 1.5.6 · `uuid` 14.0.2 · `@noble/hashes` 2.4.0 · `vitest` 5.0.1 · `jsdom` 30.1.0 · `@testing-library/react` 16.3.3 · `@testing-library/user-event` 14.6.7 · `@types/react`/`@types/react-dom` 19.3.0 · `tsx` 4.23.13 | vite 8 ตรงกับที่ vitest 5 ใน lockfile ใช้อยู่ |
| T28 | ขอบเขต "บิลวันนี้" | บิลที่ได้เลขแล้วของ **กะที่เปิดอยู่** ใหม่สุดก่อน | D22: 1 กะ = 1 วันทำการ · ตรงกับเงื่อนไขยกเลิกได้ (D47 ข้อ 2) |
| T29 | session ผู้ใช้ | เก็บในหน่วยความจำเท่านั้น · reload/ปิดแอป = ใส่ PIN ใหม่ | spec §5 ล็อกอัตโนมัติ |
| T30 | ลำดับหลังชำระ | ไปหน้า `/done` ก่อนแล้วจึงล้างตะกร้า | ถ้าล้างก่อน หน้าชำระจะเด้งกลับหน้าขาย |
| T31 | QR payload ใน DOM | `<img data-payload>` เก็บสตริง EMV ไว้ให้ e2e ตรวจยอด | ไม่ใช่ข้อมูลลับ |
| T32 | แบรนด์ในแอป (D44) | token สีใน `styles.css` · แถบบน `BrandBar` เขียวเข้ม + โลโก้ `DA-YO_reversed.svg` ที่ตัดสโลแกนด้วย `viewBox` (สูง 6032 วัดจาก PNG 1024) + คลื่นส้ม/มัทฉะ `wave.svg` · ส้ม/มัทฉะเป็นการตกแต่งเท่านั้น (contrast บนครีม 2.56:1 / 2.67:1) · เลขคิว/สถานะเลือก/โฟกัสใช้ Deep Forest · ไอคอน PWA: `appicon_cream.svg` + PNG 512 (คัดลอก) + 192 (ย่อจาก 256 ด้วย System.Drawing) · ไม่ประกาศ `maskable` เพราะขอบ 8% เล็กกว่า safe zone | D44 · WCAG 4.5:1 สำหรับตัวอักษร |
| T33 | ชนิด enum ตอน insert | แถวที่สร้างเป็นตัวแปรก่อน insert เขียน `satisfies typeof s.<table>.$inferInsert` · ไม่ cast enum | คอลัมน์ enum ของแผน 2 เป็น `textEnum` (M4) · literal ใน object ธรรมดาจะถูกขยายเป็น `string` |
| T34 | `setting` ตาม `effective_from` | `getSetting(db, key, atIso)` เลือกแถว `effective_from ≤ atIso` ล่าสุด · แถว R ที่แอปสร้างใส่ `version: 1`, `updated_at` | D47 ข้อ 8–9 |
