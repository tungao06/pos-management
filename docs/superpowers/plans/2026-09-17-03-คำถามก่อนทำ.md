# แผน 3 (pos-sell) — คำถามถึงเจ้าของก่อนเริ่มทำ

สถานะ: 🟡 รอคำตอบ · แผน: [2026-09-17-03-pos-sell.md](2026-09-17-03-pos-sell.md)

กติกา: ทุกเรื่องที่ spec และ D1–D43 ยังไม่ได้ตัดสิน ถามเจ้าของ ไม่ตัดสินเอง · แต่ละข้อมี **ค่าแนะนำ** + เหตุผลบรรทัดเดียว · ในแผน จุดที่คำตอบต้องเสียบเข้าไปเขียนว่า `(รอคำตอบ Q3-n)` · ถ้าตอบตามค่าแนะนำ โค้ดในแผนใช้ได้ทันที · ถ้าตอบต่าง ต้องแก้แผนตรงจุดที่อ้างก่อนเริ่ม task นั้น

ตอบสั้น ๆ ได้ เช่น "Q3-1: iPad Air 5 Safari · ที่เหลือตามแนะนำ"

---

## ก. อุปกรณ์และการเปิดแอป

| # | คำถาม | ค่าแนะนำ | เหตุผล | ใช้ที่ |
|---|---|---|---|---|
| Q3-1 | แท็บเล็ตที่จะใช้ขายจริงคือรุ่นอะไร ระบบอะไร (Android/iPadOS + เวอร์ชัน) และจะเปิดแอปด้วยเบราว์เซอร์อะไร | แท็บเล็ต Android + **Chrome** เวอร์ชันล่าสุด (ถ้ามี iPad อยู่แล้ว: iPadOS 17 ขึ้นไป + Safari) | Chrome รองรับ OPFS/SQLite WASM นิ่งที่สุด · spike ต้องทดสอบบนเครื่องจริงเครื่องนี้ ไม่ใช่เครื่องสมมติ | Task 1 (เป้าหมาย spike), Task 15 |
| Q3-2 | ก่อนมีแผน 5 (deploy) จะเปิดแอปบนแท็บเล็ตด้วยวิธีไหน — (ก) Cloudflare Quick Tunnel จากเครื่อง dev ชั่วคราว (ข) สมัคร Cloudflare Pages แล้ว deploy ตั้งแต่แผน 3 (ค) USB + `adb reverse` (Android เท่านั้น) | spike ใช้ **(ก)** · ถ้าจะใช้ขายจริงหลังแผน 3 ให้ทำ **(ข)** เป็นงานเล็กแยกหลังแผน 3 | OPFS ใช้ได้เฉพาะ https หรือ localhost · (ก) ไม่ต้องมีบัญชี · ขายจริงต้องมี URL ถาวร (D15 เลือก Pages ไว้แล้ว แต่ยังไม่มีแผนไหน deploy PWA ก่อนแผน 5) | Task 1 Step 9, Task 15 |

## ข. การรับเงิน

| # | คำถาม | ค่าแนะนำ | เหตุผล | ใช้ที่ |
|---|---|---|---|---|
| Q3-3 | หมายเลขพร้อมเพย์ของร้านเป็นแบบไหน (เบอร์มือถือ 10 หลัก / เลขบัตรประชาชนหรือเลขผู้เสียภาษี 13 หลัก / e-Wallet 15 หลัก) และเป็นบัญชีของใคร | กรอกในหน้า "ตั้งค่าเครื่องครั้งแรก" (ไม่ใส่ใน repo) · ใช้บัญชีที่ต้องการให้เงินเข้า | เป็นข้อมูลส่วนตัว ไม่ควรอยู่ใน git · โค้ดรองรับทั้ง 3 แบบ | Task 6 (หน้าตั้งค่า), Task 11 |
| Q3-4 | QR พร้อมเพย์: ใส่ยอดของบิลลงใน QR ทุกครั้ง (ตาม D7 และ spec §5 "Tag 29 ใส่ยอด") หรือใช้ QR คงที่ไม่มียอด ให้ลูกค้าพิมพ์ยอดเอง | **ใส่ยอดทุกบิล** (สร้างในเครื่อง ไม่ต้องใช้เน็ต) | ตรงกับ D7/§5 · ลูกค้าไม่พิมพ์ยอดผิด · ถามซ้ำเพราะคำสั่งงานเขียนว่า "static QR" ซึ่งอาจหมายถึง "สร้างเองไม่ผ่านธนาคาร" | Task 2 (`promptPayPayload`), Task 11 |
| Q3-5 | แผน 3 ไม่พิมพ์ใบเสร็จ (D4 เลื่อนการพิมพ์ไปเฟส 3) — ยืนยันว่าหลังชำระ **แสดงเลขคิว + เลขที่บิลบนจออย่างเดียว** ลูกค้าไม่ได้กระดาษ | ยืนยันตาม D4 · แสดงบนจอเท่านั้น | ยังไม่มีเครื่องพิมพ์ (D2) · schema/เลขที่บิลพร้อมสำหรับพิมพ์ภายหลัง | Task 11 (หน้าหลังชำระ) |
| Q3-6 | ส่วนลดท้ายบิล: (1) เป็นจำนวนเงินอย่างเดียว (spec §1.2) หรือมี % ด้วย (2) บังคับใส่เหตุผลเสมอไหม (3) ใครอนุมัติ — คนที่ล็อกอินอยู่ หรือต้องใส่ PIN เจ้าของซ้ำ (4) ถ้าลบรายการจนยอดต่ำกว่าส่วนลด ให้ลบส่วนลดทิ้งอัตโนมัติไหม | (1) **จำนวนเงินอย่างเดียว** (2) **บังคับเหตุผล** (3) **คนที่ล็อกอินอยู่** (ทั้งคู่เป็น owner) ไม่ต้องใส่ PIN ซ้ำ (4) **ลบส่วนลดทิ้ง** ให้ใส่ใหม่ | ตรง spec §1.2/§3.4 และ D21 (ต้องมีเหตุผล) · ร้าน 2 คนเป็นเจ้าของทั้งคู่ ใส่ PIN ซ้ำเสียเวลา · ส่วนลดเกินยอดทำให้ยอดติดลบไม่ได้ | Task 8 (reducer), Task 9 (DiscountDialog), Task 10 |

## ค. หน้าขาย

| # | คำถาม | ค่าแนะนำ | เหตุผล | ใช้ที่ |
|---|---|---|---|---|
| Q3-7 | ตะกร้าก่อนชำระ: เก็บในหน่วยความจำแล้ว **บันทึกลงฐานข้อมูลครั้งเดียวตอนกดชำระ** (event CREATED/LINE_ADDED/PAID เกิดพร้อมกัน) หรือบันทึกทุกครั้งที่เพิ่ม/ลบรายการ (spec §4.3 พูดถึง LINE_REMOVED ก่อนชำระ) | **เก็บในหน่วยความจำ บันทึกตอนชำระ** · ตะกร้าที่ล้างทิ้งไม่ถูกบันทึก | เร็ว ไม่มีบิลค้างครึ่งทาง · รายการที่ลบก่อนจ่ายไม่กระทบเงิน/สต็อก · ถ้าเลือกบันทึกทุกครั้ง ต้องเพิ่มสถานะ `open` + UPDATE บิลระหว่างขาย | Task 8, Task 10 |
| Q3-8 | แตะเมนูเดิม ขนาดเดิม ความหวานเดิมซ้ำ ให้ **รวมเป็นบรรทัดเดียวจำนวน +1** หรือแยกบรรทัดใหม่ | **รวมบรรทัด** | ตะกร้าสั้น อ่านง่ายสำหรับคนทำเครื่องดื่ม | Task 8 (reducer) |
| Q3-9 | แท็บ "ขายดี" (D27): นับจากช่วงไหน แสดงกี่เมนู | **7 วันทำการล่าสุด · 8 เมนู** · ซ่อนแท็บถ้ายังไม่มียอดขาย | 8 ปุ่มพอดีหนึ่งหน้าจอแท็บเล็ต · 7 วันตามทันเมนูตามฤดู | Task 8 (`loadMenu`) |
| Q3-10 | แตะเมนู: เปิด popup เลือกขนาด/ความหวานทุกครั้ง (spec §5) หรือแตะครั้งเดียวใส่ค่าเริ่มต้น 16 oz / 50% ทันที แล้วแตะค้างเพื่อเปิด popup | **popup ทุกครั้งตาม spec** (ค่าเริ่มต้นถูกเลือกไว้แล้ว กด "เพิ่มลงตะกร้า" ได้เลย) | ลดการสั่งผิดขนาด · ปุ่มเพิ่มอยู่ที่เดิมทุกครั้ง กดต่อเนื่องได้เร็ว | Task 9 (ItemDialog) |
| Q3-11 | ปุ่มเมนูแสดงชื่อแบบไหน: ชื่อไทย (ชาไทยเย็น) · ชื่ออังกฤษ/รหัส (Original) · หรือทั้งสอง | **ไทยตัวใหญ่ + อังกฤษตัวเล็กบรรทัดล่าง** | คนทำเครื่องดื่มจำรหัสจาก Excel ได้ ลูกค้าอ่านไทย | Task 9 |
| Q3-12 | เปิดกะ: ช่องเงินทอนตั้งต้นมีค่าเริ่มต้นไหม (เช่น ฿500) | **ไม่มีค่าเริ่มต้น ต้องกรอกเอง (0 ได้)** | บังคับให้นับจริงทุกเช้า ตัวเลขปิดกะจึงเชื่อถือได้ | Task 7 |

## ง. ยกเลิกบิล

| # | คำถาม | ค่าแนะนำ | เหตุผล | ใช้ที่ |
|---|---|---|---|---|
| Q3-13 | ยกเลิกได้เฉพาะบิลใน **กะที่เปิดอยู่** หรือย้อนไปบิลของวัน/กะที่ปิดแล้วได้ | **เฉพาะกะที่เปิดอยู่** | เงินคืน (`VOID_REFUND`) ต้องออกจากลิ้นชักของกะนี้ · Z report ของกะที่ปิดแล้วห้ามเปลี่ยน (§4.8) · คืนเงินย้อนหลังให้ทำด้วยมือไปก่อน | Task 13 |
| Q3-14 | เหตุผลการยกเลิก: มีปุ่มเหตุผลสำเร็จรูป + พิมพ์เองได้ไหม และมีข้อไหนบ้าง | ปุ่ม: **"ลูกค้าเปลี่ยนใจ" · "กดผิดเมนู" · "ทำผิดสูตร"** + ช่องพิมพ์เอง (ต้องมีข้อความเสมอ) | กดเร็ว · รายงานยกเลิก (แผน 7) จัดกลุ่มได้ | Task 3 (`th.ts`), Task 13 |
| Q3-15 | ยกเลิกบิลที่จ่ายด้วย QR: **บังคับ** ใส่เลขอ้างอิงการโอนคืนไหม | **บังคับ** | spec §4.3 "บันทึกว่าโอนคืนแล้ว (อ้างอิงใส่เอง)" · กันลืมโอนคืน · ถ้ายังไม่ได้โอน ให้ยังไม่กดยกเลิก | Task 13 |

## จ. ข้อมูลเริ่มต้นและขอบเขต

| # | คำถาม | ค่าแนะนำ | เหตุผล | ใช้ที่ |
|---|---|---|---|---|
| Q3-16 | ยังไม่มีเซิร์ฟเวอร์ (แผน 5) — สร้างผู้ใช้และเครื่องอย่างไร | หน้า **"ตั้งค่าเครื่องครั้งแรก"** บนแท็บเล็ต: ชื่อเครื่อง · prefix เลขบิล `A` · เจ้าของ 2 คน **TungAo, DCm** (role owner) + PIN 4–6 หลัก · หมายเลขพร้อมเพย์ · แผน 5 อัปโหลดผู้ใช้/เครื่องเหล่านี้ขึ้นเซิร์ฟเวอร์ | spec §1.3 ระบุ 2 เจ้าของ · ขายได้ทันทีโดยไม่รอเซิร์ฟเวอร์ (§11 ขั้น 3) | Task 6 |
| Q3-17 | README แผนบอกว่าแผน 3 รวม **ปิดกะ/นับเงิน/Z report** ด้วย แต่แผนนี้ครอบคลุมถึง "ขาย · จ่าย · ยกเลิก · ประวัติ" เพื่อให้แผนไม่ยาวเกิน — แยกปิดกะ/Z + ปุ่มสำรองไฟล์ฐานข้อมูล (§11 "export ไฟล์ SQLite ทุกคืนชั่วคราว") เป็น **แผน 3b** ต่อทันทีได้ไหม | **แยกเป็นแผน 3b** และ **ยังไม่ใช้ขายจริงจนกว่า 3b เสร็จ** | ร้านต้องปิดกะได้และมีสำรองข้อมูลก่อนใช้จริง · แผนเดียว 20+ task ยากต่อการ review | ทั้งแผน (ขอบเขต) |
| Q3-18 | ข้อความภาษาไทยบนจอ (ตาราง `TH` ใน Task 3 ของแผน) — ใช้ตามที่ร่างไว้ได้ไหม หรือจะแก้คำไหน | **ใช้ตามร่าง** แก้ทีหลังได้ที่ไฟล์เดียว | ข้อความทั้งหมดอยู่ใน `apps/pos/src/ui/th.ts` ไฟล์เดียว e2e อ้าง `data-testid` ไม่อ้างข้อความ | Task 3 |
| Q3-19 | สีหลัก/โลโก้ของแอป | **สีส้มชาไทย `#c2410c` ไม่มีโลโก้** (ไอคอนตัวอักษร "DA") | ยังไม่มีไฟล์แบรนด์ใน repo · เปลี่ยนได้ที่ `styles.css` + `public/icon.svg` | Task 3 |

---

## ตัดสินใจเชิงเทคนิค (ไม่ต้องตอบ — ตามมาจาก spec หรือเป็นเรื่องภายในโค้ด · คัดค้านได้)

| # | เรื่อง | ตัดสินใจ | เหตุผล/ที่มา |
|---|---|---|---|
| T1 | VFS ของ SQLite WASM | `opfs-sahpool` (`installOpfsSAHPoolVfs`) เป็นหลัก | ไม่ต้องตั้ง header COOP/COEP · เร็ว · ใช้ได้บน Safari 16.4+ และ Chrome · ข้อเสีย: เปิดได้แท็บเดียว (แอปขายใช้แท็บเดียวอยู่แล้ว) · ทางสำรองอยู่ใน Task 1 |
| T2 | Drizzle ในเครื่อง | `drizzle-orm/sqlite-proxy` ทั้งใน Worker (ครอบ oo1 ของ sqlite-wasm) และในเทสต์ (ครอบ `node:sqlite`) → โค้ด repository ชุดเดียวกันทั้งสองที่ | ตรวจแล้ว: `transaction()` ของ sqlite-proxy ส่ง `begin/commit/rollback` ผ่าน callback · rollback ทำงานจริง (ทดลองกับ drizzle 0.45.2) |
| T3 | ทุกคำสั่งรันเป็นคิวทีละคำสั่ง | `createSerialQueue()` ครอบทุกเมธอดของ API | connection เดียว + transaction แบบ BEGIN/COMMIT ห้ามซ้อนกัน |
| T4 | ตรรกะทั้งหมดรันใน Worker | UI เรียก `PosApi` ผ่าน Comlink · 1 การกดของผู้ใช้ = 1 round-trip = 1 transaction | §6.1 "ทุกการเขียนเป็น transaction เดียว" · ไม่ข้าม thread ทีละ query |
| T5 | migration ฝั่งเบราว์เซอร์ | สคริปต์สร้างไฟล์ `sqlite-migrations.gen.ts` จาก `drizzle/sqlite` (commit ไว้ + เทสต์ตรวจความสด) · ตาราง `__dayo_migrations` · 1 migration = 1 transaction | ใช้ได้ทั้ง Vite และ Node test โดยไม่พึ่ง `?raw` |
| T6 | id ของข้อมูลอ้างอิงจาก seed | `deterministicIds('dayo-seed-v1')` (UUID v8 จาก sha256 ของลำดับ) + `effectiveFrom` คงที่ `2026-01-01T00:00:00.000Z` | เครื่องและเซิร์ฟเวอร์ (แผน 5) ต้องได้ id ชุดเดียวกันจาก seed เดียวกัน ไม่งั้นธุรกรรมที่ sync ขึ้นไปอ้าง id ไม่ตรง · **แผน 5 ต้องใช้ฟังก์ชันเดียวกัน** |
| T7 | ตัวตนของเครื่องนี้ | `sync_state` key `local.device_id` | ตาราง `device` จะมีหลายเครื่องหลัง sync |
| T8 | แฮช PIN | argon2id (`@noble/hashes`) t=2, m=19456 KiB, p=1, salt 16 ไบต์ เก็บเป็นสตริง `argon2id$t=2,m=19456,p=1$<salt>$<hash>` · เทสต์ใช้ t=1, m=64 | spec §3.1 ระบุ argon2 · ค่า OWASP ขั้นต่ำ · spike วัดเวลาบนแท็บเล็ต |
| T9 | เลขที่บิล | อ่าน `receipt_no` ล่าสุดของเครื่องใน transaction เดียวกับการ insert บิล (`nextReceiptNo`) ไม่มีตาราง counter | Worker เป็นผู้เขียนคนเดียว + คิวทีละคำสั่ง → ไม่ชน · rollback ไม่กินเลข (ไม่มีช่องว่าง §4.7) |
| T10 | เลขคิว | `max(queue_no)` ของ (เครื่อง, business_date) + 1 | §4.7 รีเซ็ตทุกวันทำการ |
| T11 | business_date | วันที่ตามเวลาไทย (Asia/Bangkok) ตอนเปิดกะ · บิลใช้ `shift.business_date` | §4.7 "ขายหลังเที่ยงคืนยังเป็นวันเดิม" |
| T12 | movement `SALE` | รวมต่อ item ต่อบิล (ไม่แยกต่อบรรทัด) | ลดจำนวนแถว · ตรวจ invariant 4 ได้เพราะแต่ละบรรทัดเก็บ `recipe_id` |
| T13 | `order.cost_satang` | Σ (`unit_cost_satang` × qty) ของทุกบรรทัด | ให้ตรงกับ snapshot ต่อบรรทัด |
| T14 | UPDATE ที่อนุญาต | เฉพาะ `order.status` (paid→voided) + `order.voided_at` + upsert แคช `item_cost_state` · อย่างอื่น insert อย่างเดียว · การเปลี่ยนสถานะเข้า outbox เป็นแถวใหม่ key `order:<id>:voided` | spec §3.4 มีคอลัมน์สถานะ/เวลายกเลิก และ §6.5 ให้เครื่องเปลี่ยนสถานะแล้ว push · ความจริงของประวัติอยู่ที่ `order_event` |
| T15 | outbox | เฉพาะตาราง T (`order`, `order_line`, `payment`, `discount`, `order_event`, `stock_movement`, `shift`, `cash_movement`) · `idempotency_key` = `<table>:<id>` · `row_json` = แถวที่ insert (key camelCase) · ผู้ใช้/เครื่อง/ตั้งค่าที่สร้างตอนตั้งค่าเครื่องไม่เข้า outbox — **แผน 5 ตัดสินวิธีลงทะเบียน** | §6.1 · แผน 3 เขียนอย่างเดียว ไม่ส่ง |
| T16 | event ตอนขาย | `CREATED` → `LINE_ADDED` × n → `DISCOUNT_APPLIED` (ถ้ามี) → `PAID` → `STOCK_DEDUCTED` · ยกเลิก: `VOIDED` (payload มี `made`, `waste`, `refund`) → `STOCK_RETURNED` (ถ้ายังไม่ทำ) · โซ่ต่อเครื่อง `chain_id = device_id` | §3.4, §4.9, D38, D39 · ขึ้นกับ Q3-7 |
| T17 | commitSale ซ้ำ | `orderId` สร้างตอนเริ่มตะกร้า · ส่งซ้ำ (กดสองครั้ง/Worker ตอบช้า) คืนผลเดิม ไม่สร้างบิลใหม่ | กันบิลซ้ำ |
| T18 | PromptPay payload | เขียนเองใน `packages/domain/src/promptpay.ts` (EMVCo + CRC-16/CCITT-FALSE) · ค่าทดสอบตรวจเทียบกับไลบรารี `promptpay-qr` แล้ว 4 ชุด (รวมตัวอย่างใน README ของไลบรารี) | logic เงินอยู่ใน domain · ไม่เพิ่ม dependency runtime |
| T19 | วาด QR | `qrcode` 1.5.4 `toDataURL` | เล็ก ใช้ในเบราว์เซอร์ได้ ไม่ต้องใช้เน็ต |
| T20 | PWA ออฟไลน์ | `vite-plugin-pwa` 1.3.0 precache ทุกไฟล์รวม `.wasm` (สูงสุด 8 MB) · `registerType: 'autoUpdate'` | เปิดแอปได้ตอนไม่มีเน็ต |
| T21 | React Query | `networkMode: 'always'` | ค่าเริ่มต้นจะหยุด query ตอนออฟไลน์ ทั้งที่ข้อมูลอยู่ในเครื่อง |
| T22 | Router | TanStack Router แบบ code-based (ไม่ใช้ file-based plugin) | เครื่องมือน้อยลง · 10 หน้า |
| T23 | error ข้าม Worker | `PosError` ข้อความขึ้นต้น `CODE:` · UI แปลงเป็นข้อความไทยที่ `ui/errors.ts` | Comlink ส่งแค่ message/name/stack |
| T24 | เงินคืนตอนยกเลิกเงินสด | `cash_movement` kind `VOID_REFUND` ผูกกะที่เปิดอยู่ จำนวน = Σ payment CASH | D36 |
| T25 | เครื่องมือทดสอบ | unit + integration: Vitest 5 + `node:sqlite` ในหน่วยความจำ (ไม่ต้อง build native) · component: Testing Library + jsdom · e2e: Playwright Chromium (CI) · เครื่องจริง: checklist มือ | better-sqlite3 build ไม่ผ่านบน Windows ที่ไม่มี Visual Studio (ลองแล้ว) · `node:sqlite` ต้อง Node ≥ 22.13 (ตรวจบน 22.23.2) |
| T26 | แอป spike | เก็บไว้ที่ `apps/pos-spike` ใช้ทดสอบซ้ำเมื่อเปลี่ยนแท็บเล็ต/เบราว์เซอร์ · ไม่อยู่ใน `pnpm test` | spike คือเกณฑ์รับเครื่อง ไม่ใช่โค้ดทิ้ง |
| T27 | เวอร์ชันที่ pin (ตรวจด้วย `pnpm view` วันที่ 2026-09-17) | `@sqlite.org/sqlite-wasm` 3.53.4-build1 · `drizzle-orm` 0.45.2 · `react`/`react-dom` 19.3.0 · `@tanstack/react-router` 1.170.38 · `@tanstack/react-query` 5.103.1 · `vite` 8.3.0 · `@vitejs/plugin-react` 6.1.1 · `vite-plugin-pwa` 1.3.0 · `@playwright/test` 1.63.0 · `comlink` 4.4.2 · `qrcode` 1.5.4 · `@types/qrcode` 1.5.6 · `uuid` 14.0.2 · `@noble/hashes` 2.4.0 · `vitest` 5.0.1 · `jsdom` 30.1.0 · `@testing-library/react` 16.3.3 · `@testing-library/user-event` 14.6.7 · `@types/react`/`@types/react-dom` 19.3.0 · `tsx` 4.23.13 | vite 8 ตรงกับที่ vitest 5 ใน lockfile ใช้อยู่ · plugin-pwa 1.3 และ plugin-react 6.1 รองรับ vite 8 (peer deps) |
